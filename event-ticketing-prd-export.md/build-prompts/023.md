# Step 23 of 345: Set up audit logging and compliance feature structure

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_audit-logging-compliance

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an audit logging and compliance system for regulatory requirements. This is an admin-only feature that tracks all system transactions, user actions, and data modifications.

On the backend (Laravel), create a feature folder at app/Features/Compliance/ with subdirectories for: Models (AuditLog model), Controllers (AuditLogController, ComplianceReportController), Services (AuditLogService, ComplianceReportService, ExportService), Requests (validation form requests), and Jobs (for async report generation). Also create a database/migrations folder for the auditLogs table migration.

On the frontend (React + Vite), create a compliance feature area at src/features/compliance/ with subdirectories for: pages (AuditLogsViewerPage, ComplianceReportsPage), components (AuditLogsTable, ReportGenerator, ComplianceChecklist, FilterPanel, ExportButton, BulkActionBar), hooks (useAuditLogs, useComplianceReports, useExport), types (audit log types, report types), and services (API client functions).

This feature has two pages and multiple reusable components, so keeping pages and components separate will keep things organized. The backend needs service classes to handle the complex business logic around filtering, exporting, and report generation.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] Backend compliance feature folder exists with Models, Controllers, Services, Requests, and Jobs subdirectories
- [ ] Frontend compliance feature folder exists with pages, components, hooks, types, and services subdirectories
- [ ] The Laravel project still builds without errors
- [ ] The React project still builds without errors

Test the actual flow: Run `php artisan` and `npm run dev` to make sure both projects compile cleanly.

Then give me your honest advice on:
- Whether this folder structure makes sense for the complexity of audit logging and compliance reporting
- If there's a better way to organize the backend services given Laravel conventions
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: