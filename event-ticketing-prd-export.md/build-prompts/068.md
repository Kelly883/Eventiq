# Step 68 of 345: Create delivery_events and delivery_preferences tables

================================================================================

**Category:** Database Setup
**Flow:** feature_ticket-delivery-distribution

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to create two new database tables to store delivery tracking and user preferences.

Create a Laravel migration for the delivery_events table. This table is an immutable audit trail of every ticket delivery attempt. Fields: id (UUID primary key), ticketId (UUID, foreign key to tickets table, indexed), orderId (UUID, foreign key to orders table, indexed), userId (UUID, foreign key to users table, indexed), eventId (UUID, foreign key to events table, indexed), deliveryMethod (enum: 'email', 'sms', 'dashboard'), recipientEmail (string, nullable, hashed), recipientPhone (string, nullable, hashed), status (enum: 'pending', 'sent', 'failed', 'bounced', 'viewed'), deliveryTimestamp (timestamp, nullable), viewedTimestamp (timestamp, nullable), attemptCount (integer, default 1), maxAttempts (integer, default 3), lastAttemptAt (timestamp, nullable), nextRetryAt (timestamp, nullable), errorMessage (text, nullable), errorCode (string, nullable), providerResponse (json, nullable), fraudEventId (UUID, nullable, foreign key), deliveryBlocked (boolean, default false), blockReason (string, nullable), qrCodeData (text), ticketPdfUrl (string, nullable), createdAt and updatedAt timestamps. Add indexes on ticketId, userId, eventId, and status for fast lookups.

Create a migration for the delivery_preferences table. This stores user settings for how they want tickets delivered. Fields: id (UUID primary key), userId (UUID, unique, foreign key to users table, indexed), emailEnabled (boolean, default true), emailAddress (string, nullable), emailVerified (boolean, default false), smsEnabled (boolean, default false), phoneNumber (string, nullable, E.164 format), phoneVerified (boolean, default false), dashboardEnabled (boolean, default true), primaryMethod (enum: 'email', 'sms', 'dashboard', default 'email'), backupMethod (enum: 'email', 'sms', 'dashboard', nullable), deliveryTiming (enum: 'immediate', 'scheduled_1h_before', 'scheduled_24h_before', default 'immediate'), receiveConfirmation (boolean, default true), receiveReminders (boolean, default true), languagePreference (enum: 'en', 'es', 'fr', 'de', default 'en'), createdAt and updatedAt timestamps.

Run php artisan migrate to apply both migrations to your database.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both tables are created correctly with proper indexes and constraints.

Confirm that:
- [ ] The delivery_events table exists with all 25 fields
- [ ] The delivery_preferences table exists with all 17 fields
- [ ] Indexes are created on delivery_events: ticketId, userId, eventId, status
- [ ] Unique index exists on delivery_preferences.userId (one-to-one relationship)
- [ ] Foreign key constraints are in place for all relationships
- [ ] Enum columns use proper database enum types or string constraints
- [ ] Timestamps (createdAt, updatedAt) are properly configured

Test the actual flow: Run php artisan migrate. Then use a database client to inspect the tables — verify all columns exist with correct types, indexes are present, and foreign keys are set up.

Then give me your honest advice on:
- Whether the indexes cover all the queries we'll need (filtering by status, user, event)
- If there are any missing constraints (e.g., should emailAddress be unique per user?)
- Performance concerns with the delivery_events table as it grows (millions of rows)
- Whether soft deletes should be enabled for audit trail purposes
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: