# Step 341 of 345: Test the push notification feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_push-notifications

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the push notification feature doesn't break.

Unit tests:
- Delivery preferences state: loading preferences populates all five toggle states, toggling a preference updates the local state optimistically, failed requests revert the state, 401 errors are caught.
- Push templates state: loading templates populates the list, selecting a template loads it into the editor, creating a new template clears the editor, saving/updating/deleting templates make the right API calls.
- Test push notification state: sending a test makes a POST request, success and error states are captured.

Component tests (React Testing Library):
- PushNotificationSettingsSection: master toggle disables sub-toggles when OFF, toggling a switch calls the state manager, error messages appear and disappear, loading spinner shows while a request is in flight.
- AdminPushTemplateManagementPage: template list renders, clicking a template loads it into the editor, character counters work for title (max 65) and body (max 178), Save button POSTs/PATCHes, Delete button DELETEs, Create Template button clears the editor, Send Test button opens a modal.
- SendTestPushModal: accepts a User ID, sends a POST request when submitted, shows success/error messages.

Integration tests (full user flows):
- User flow: Open settings → see Push Notifications section → toggle master ON → browser asks for permission → grant it → preference saves → toggle a sub-toggle → it saves immediately → toggle master OFF → all sub-toggles disable.
- Admin flow: Open /admin/settings/push-templates → see template list → click a template → editor loads it → edit the title and body → click Save → template updates in the list → click Create Template → editor clears → fill in a new template → click Save → new template appears → click Send Test → enter a user ID → test sends → success message → click Delete → template is removed.
- Error flow: Open settings → turn off WiFi → toggle a preference → toggle reverts and shows error → turn WiFi on → click Retry → preference saves.

API endpoint tests:
- POST /api/notifications/device-tokens: valid request with auth token registers a device, missing token returns 401, invalid device_type returns 400.
- DELETE /api/notifications/device-tokens: valid request with auth token deletes a device, missing token returns 401.
- GET /api/admin/push-templates: valid request with admin token returns all templates, non-admin returns 403, missing token returns 401.
- POST /api/admin/push-templates: valid request creates a template, title longer than 65 chars returns 422, non-admin returns 403.
- PATCH /api/admin/push-templates/:templateId: valid request updates a template, non-existent template returns 404, non-admin returns 403.
- DELETE /api/admin/push-templates/:templateId: valid request deletes a template, non-existent template returns 404, non-admin returns 403.
- POST /api/admin/push-templates/send-test: valid request with admin token sends a test, non-existent user returns 404, non-admin returns 403.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: