# Step 327 of 345: Test the inventory management feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_ticket-inventory-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the inventory feature doesn't break and works as expected.

Unit tests:
- Error handler: each error code (400, 401, 403, 404, 409, 429, 500) maps to the right friendly message
- Query hooks: useInventorySummary fetches and caches correctly, useInventoryDetails refetches when filter/sort changes, useAdjustInventory invalidates other queries on success

Component tests:
- InventorySummaryCards: displays totalCapacity, totalSold, totalAvailable, lowStockTierCount correctly
- InventoryTable: shows all tiers with correct columns, expandable rows show pricing windows, sort dropdown works, filter works
- AdjustInventoryModal: dropdown selects tier/window, numeric input validates (no negatives, no exceeding capacity), current vs. proposed quantities display side-by-side, Apply button disabled until quantity changes, Apply button submits and closes modal, Cancel button closes without changes
- LowStockAlert: displays with correct color (green/amber/red) based on utilization

Integration tests (full user flows):
- Open dashboard → summary cards load → tier table loads → expand a tier → see pricing windows
- Filter by tier → table updates → sort by allocated → table reorders
- Click 'Adjust Inventory' → modal opens → select a tier → change quantity → click Apply → success toast → modal closes → dashboard refreshes with new quantity
- Try adjusting to exceed capacity → see error message → quantity doesn't change
- Click 'Export' → CSV file downloads with all inventory data
- Simulate 401 (session expired) → see 'Session expired' toast → redirected to login
- Simulate 403 (access denied) → see 'Access Denied' message
- Simulate 500 (server error) → see error banner with Retry → click Retry → data loads

Also test the API endpoints: make sure all five return the right responses with valid auth, reject without auth, enforce event ownership, validate input, and log adjustments to inventory_adjustments.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (error handler, query hooks)
- [ ] All component tests pass (summary cards, table, modal, alerts)
- [ ] All integration tests pass on a real device
- [ ] All API endpoint tests pass (valid auth, no auth, event ownership, input validation, audit logging)
- [ ] Test coverage is decent (components, hooks, pages, and API endpoints should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: open dashboard → summary and table load → expand a tier → see pricing windows → filter by tier → table updates → sort by allocated → table reorders → click 'Adjust Inventory' → modal opens → select a tier → change quantity → click Apply → success toast → modal closes → dashboard refreshes with new quantity. Try adjusting to exceed capacity → see error message. Export the inventory → CSV downloads. Simulate errors (401, 403, 500) → see appropriate error messages and recovery options.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important organizer workflows
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Whether the API endpoint tests are comprehensive enough
- Tests you'd add before feeling confident about a release to real organizers
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: