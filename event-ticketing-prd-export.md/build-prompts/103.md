# Step 103 of 345: Create TypeScript types and Laravel models for admin data

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_admin-panel-platform-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to define the data structures for the admin panel so the frontend and backend speak the same language.

Frontend TypeScript types at src/features/admin/types/:

1. Create admin.types.ts with interfaces:
- AdminUser: { id, email, name, role ('admin'|'organizer'|'attendee'), status ('active'|'suspended'|'pending'), registeredAt, lastLoginAt, suspensionReason?, suspensionDate? }
- AdminEvent: { id, title, organizerId, organizerName, status ('published'|'draft'|'cancelled'|'flagged'), attendeeCount, ticketsSold, revenue, createdAt, flagReason?, flagDate? }
- AdminPayment: { id, orderId, amount, currency, paymentMethod, status ('completed'|'pending'|'failed'|'refunded'), gatewayResponseCode, timestamp, buyerEmail, fraudRiskScore, fraudDetectionMethod? }
- AdminSettings: { id, settingKey, settingValue, description, category ('platform'|'fraud'|'payments'|'notifications'|'system'), isEditable, lastModifiedBy, lastModifiedAt }
- AuditLog: { id, userId, action, targetType ('user'|'event'|'order'|'payout'), targetId, description, metadata, createdAt, performedByName }
- DashboardMetrics: { period, totalRevenue, activeEvents, pendingApprovals, flaggedTransactions, failedPayouts, revenueTrend ('up'|'down'|'flat'), eventsTrend, transactionsTrend }
- DashboardAlert: { id, type ('payment_failure'|'fraud_spike'|'system_error'|'payout_failure'|'low_inventory'), severity ('critical'|'warning'|'info'), title, description, actionUrl, timestamp }

2. Create api-responses.types.ts with API response shapes for all endpoints.

Backend Laravel models at app/Models/:

1. Create AdminSetting model with:
- Table: admin_settings
- Fillable: ['settingKey', 'settingValue', 'description', 'category', 'isEditable', 'lastModifiedBy']
- Casts: ['settingValue' => 'json', 'isEditable' => 'boolean']
- Relationships: belongsTo(User, 'lastModifiedBy')
- Scopes: byCategory($category), editable(), byKey($key)

2. Create AuditLog model with:
- Table: auditLogs
- Fillable: ['userId', 'action', 'targetType', 'targetId', 'description', 'metadata']
- Casts: ['metadata' => 'json']
- Relationships: belongsTo(User, 'userId')
- Scopes: byAction($action), byAdmin($userId), recent($days = 7)

3. Ensure existing models (User, Event, Payment, Payout, FraudEvent) have proper relationships and scopes:
- User: hasMany(AuditLog), scope for role and status
- Event: scope for status filtering
- Payment: scope for status and method filtering
- Payout: scope for status filtering
- FraudEvent: scope for riskScore and status filtering

Add helper methods to models for formatting (e.g., User::getStatusBadgeColor(), Payment::getStatusLabel()).

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all types and models are properly defined.

Confirm that:
- [ ] All TypeScript interfaces are defined and exported from admin.types.ts
- [ ] API response types match the actual endpoint responses
- [ ] AdminSetting model has all required fields and relationships
- [ ] AuditLog model has all required fields and relationships
- [ ] User model has role and status fields with proper scopes
- [ ] Event model has status field with proper scopes
- [ ] Payment model has status and paymentMethod fields with proper scopes
- [ ] All models compile without TypeScript errors
- [ ] Laravel models can be instantiated and queried without errors

Test the actual flow: In TypeScript, import and use the types — no errors should appear. In Laravel, query AdminSetting::byCategory('platform')->get() — should work. Query AuditLog::byAction('user_suspended')->recent(7)->get() — should work.

Then give me your honest advice on:
- Whether the types cover all the data the admin pages will display
- If the model relationships are complete and efficient
- Any fields missing that would be needed for real admin operations
- Performance concerns with the scopes and relationships
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: