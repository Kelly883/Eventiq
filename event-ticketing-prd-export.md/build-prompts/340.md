# Step 340 of 345: Test the check-in feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_attendee-check-in-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the check-in feature doesn't break and works under realistic conditions.

Unit tests:
- SearchState: debounces search queries (typing quickly only triggers one API call), stores recent searches, clears search
- CheckInStatsState: fetches stats, auto-refreshes every 5 seconds, uses lastUpdateAt for incremental updates
- OfflineSyncState: queues check-ins when offline, syncs when online, handles conflicts
- BulkCheckInState: processes multiple tickets, tracks progress, summarizes results
- Error handler: maps API error codes to friendly messages (401 → redirect to login, 403 → 'Access Denied', 429 → rate limit message, etc.)

Component tests:
- CheckInDashboardPage: loads stats, displays them, search bar works, check-in button works, offline banner appears/disappears
- BulkCheckInPage: accepts multiple ticket IDs, processes them, shows summary
- SearchResultsPanel: displays results, shows status badges, check-in button works
- ErrorNotification: displays error messages, has a close button, auto-dismisses after 5 seconds

Integration tests (full user flows):
- Happy path: Open CheckInDashboardPage → search for a ticket → see result → click check-in → see success toast and stats update
- Bulk check-in: Go to BulkCheckInPage → paste 10 ticket IDs → click 'Check In All' → see progress → see summary (X succeeded, Y failed)
- Offline sync: Turn off WiFi → check in a ticket → see offline banner → turn WiFi back on → see sync confirmation
- Error handling: Trigger a 401 (auth expired) → redirect to login. Trigger a 429 (rate limit) → see countdown timer. Trigger a 400 (bad input) → see error message.
- Rate limits: Spam search 100 times → see rate limit message after 60 searches. Spam bulk check-in 50 times → see rate limit message after 30 operations.

Also test the API endpoints themselves:
- GET /api/venue/check-in/search: returns correct results, respects limit (10 items), filters by eventId, requires auth
- POST /api/venue/check-in/bulk: checks in tickets, returns success/failure per ticket, is idempotent, requires auth, rate limited
- GET /api/venue/check-in/stats: returns correct stats, uses lastUpdateAt for incremental updates, requires auth
- POST /api/venue/check-in/detect-duplicate: detects duplicates, returns risk level, requires auth
- POST /api/venue/check-in/offline-sync: syncs queued check-ins, handles conflicts, requires auth
- GET /api/venue/check-in/export: exports data as CSV or JSON, respects filters, requires auth

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: