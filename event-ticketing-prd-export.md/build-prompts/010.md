# Step 10 of 345: Set up folder structure for event discovery

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_event-discovery-browsing

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an event discovery and browsing feature for a React + Laravel event marketplace. For this step, I need you to set up the folder structure that will organize all the code.

On the frontend (React + Vite), create a features/events folder with these subfolders:
- pages (we'll have EventBrowsePage, EventDetailPage, and CategoryBrowsePage)
- components (reusable UI pieces like EventCard, FilterSidebar, SearchBar, TicketTierCard, etc.)
- hooks (custom React hooks for data fetching and state management)
- types (TypeScript interfaces for events, filters, pricing, etc.)
- services (API client functions that call the Laravel backend)
- utils (helper functions for formatting, filtering, sorting)

On the backend (Laravel), create a folder structure under app/Http/Controllers for the event discovery API:
- EventPublicController (handles the public event endpoints)
- Create a routes/api.php section for public event routes (these don't require authentication)

Also create a Models folder if it doesn't exist, where we'll define the Event, Organizer, TicketTier, and related models.

This structure keeps the event discovery feature isolated and makes it easy to add more features later without cluttering the codebase.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The frontend features/events folder exists with pages/, components/, hooks/, types/, services/, and utils/ subfolders
- [ ] The Laravel app/Http/Controllers folder exists and is ready for EventPublicController
- [ ] The Laravel app/Models folder exists and is ready for Event, Organizer, and TicketTier models
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Just run a build on the frontend (npm run build or yarn build) and make sure nothing is broken — this is just scaffolding.

Then give me your honest advice on:
- Whether this folder structure makes sense for the number of files we'll have
- If there's a better way to organize things given the project's existing structure
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: