# Step 78 of 345: Set up database schema for offline sync and device tokens

================================================================================

**Category:** Database Setup
**Flow:** feature_mobile-app-responsive-design

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the Laravel database to support offline ticket syncing and device token tracking.

First, ensure the push_notification_devices table exists with these fields (based on the push_notification_devices schema): id (uuid, primary key), user_id (foreign key to users), token (string, unique), offline_enabled (boolean, default false), last_sync_at (timestamp, nullable), created_at, updated_at. Add an index on (user_id, token) for fast lookups when a device syncs.

Second, create a migration that adds an index on the events table for (start_date) so we can efficiently query future events. Also add an index on tickets (user_id, event_id) for fast filtering of user tickets by event.

Run the migrations: `php artisan migrate`. After migration, seed a test user and a test push_notification_devices record so you can test the endpoints locally.

Set up Laravel policies for the push_notification_devices model: Create policy allows if the user is authenticated. Read allows if the user is authenticated. Update allows if the device belongs to the authenticated user. Delete allows if the device belongs to the authenticated user. The createdAt field is immutable.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database is properly configured for offline sync.

Confirm that:
- [ ] The push_notification_devices table exists with all fields: id, user_id, token, offline_enabled, last_sync_at, created_at, updated_at
- [ ] Indexes exist on (user_id, token), events (start_date), and tickets (user_id, event_id)
- [ ] Migrations run without errors
- [ ] A test user and device record exist after seeding
- [ ] The PushNotificationDevicePolicy enforces ownership correctly

Test the actual flow: Run `php artisan migrate:fresh --seed`. Then in tinker, query a test device: `PushNotificationDevice::where('user_id', 1)->first()` — it should return the seeded device. Verify the indexes exist: `php artisan tinker` → `DB::select('SHOW INDEXES FROM push_notification_devices')`.

Then give me your honest advice on:
- Whether the indexes cover all the queries we'll need
- Any security gaps in the policy (can a user see other users' devices?)
- Edge cases (device with no user, orphaned devices after user deletion)
- Anything you'd tighten up before going live
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: