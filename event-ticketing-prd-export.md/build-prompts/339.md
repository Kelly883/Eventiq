# Step 339 of 345: Test the dashboard and ticket feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_user-dashboard-ticket-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the dashboard and ticket feature doesn't break.

**Unit Tests:**
- Dashboard preferences: loading preferences works, updating preferences works, defaults are applied
- Error handler: maps API error codes to user-friendly messages
- Date range filters: 'upcoming' correctly identifies future events, 'past' correctly identifies past events
- Delivery status badge: color-coding is correct (green=sent, amber=pending, red=failed)

**Component/Widget Tests:**
- UserDashboardPage: loading state shows skeleton cards, empty state shows 'Start your event journey', metrics display correctly, activity feed shows last 5 purchases
- UserTicketsDashboardPage: tickets are grouped by event, collapsible sections work, filter chips update the list, search works, delivery status badges are color-coded
- TicketDetailPage: QR code displays and is scannable, event details show correctly, delivery timeline displays all attempts, action buttons are present
- Ticket card: shows tier name, ticket ID, delivery status badge, action buttons
- Delivery timeline: shows all delivery attempts with timestamps and outcomes

**Integration Tests (full user flows):**
- Browse dashboard → see metrics and activity → click 'View Tickets' → see UserTicketsDashboardPage → filter by 'upcoming' → see only upcoming events

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: