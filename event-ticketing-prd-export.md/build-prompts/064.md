# Step 64 of 345: Create database migrations and indexes for events

================================================================================

**Category:** Database Setup
**Flow:** feature_event-discovery-browsing

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the database schema and indexes so event queries run fast and filtering works smoothly.

Create a Laravel migration for the events table with all the fields from the events schema: id (UUID primary key), status (enum: draft, published, archived), title (string, indexed for search), description (longText), category (string, indexed for filtering), banner_image_url (string), start_datetime (timestamp, indexed for date range queries), end_datetime (timestamp), venue_name (string), venue_address (string, indexed for location search), venue_latitude (decimal), venue_longitude (decimal), capacity (integer), organizer_id (UUID, foreign key, indexed), user_id (UUID, foreign key), createdAt (timestamp), updatedAt (timestamp), deleted_at (timestamp for soft deletes).

Create indexes on:
- status (for filtering published events)
- category (for category filtering)
- start_datetime (for date range queries)
- organizer_id (for finding events by organizer)
- venue_address (for location search)
- A composite index on (status, start_datetime) for the common query of published events sorted by date
- A composite index on (status, category) for category browsing

Run the migration with php artisan migrate. Also create migrations for related tables: organizers, ticket_tiers, pricing_windows, ticket_inventory, and analytics_events_metrics (these are referenced by the endpoints but we're setting them up now so the relationships work).

Set up soft deletes on the Event model so deleted events are retained in the database but hidden from queries.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database is properly set up with all the right indexes.

Confirm that:
- [ ] The events table exists with all fields from the schema
- [ ] All indexes are created (status, category, start_datetime, organizer_id, venue_address, and composite indexes)
- [ ] Soft deletes are enabled on the Event model
- [ ] Related tables (organizers, ticket_tiers, pricing_windows, ticket_inventory, analytics_events_metrics) exist
- [ ] Foreign key relationships are set up correctly
- [ ] Running php artisan migrate completes without errors

Test the actual flow: Run php artisan migrate. Then use a database client (like TablePlus or phpMyAdmin) to verify the events table structure and indexes. Run a test query like SELECT * FROM events WHERE status='published' ORDER BY start_datetime DESC and check that it uses the composite index.

Then give me your honest advice on:
- Whether the indexes cover all the queries we'll need (search, filtering, sorting)
- If there are any missing indexes that would slow down queries
- How this will perform with thousands of events
- Any database design issues before we start querying
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: