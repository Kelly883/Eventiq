# Step 251 of 345: Wire ticket tiers into event editor

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_tiered-ticketing-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the ticket tier management to the existing event editor.

Add a 'Ticket Tiers' tab or section to the event editor page. When an organizer clicks this tab, load the ticket tier management interface. The tab should show a badge with the number of tiers currently configured (e.g., 'Ticket Tiers (3)').

When the organizer navigates away from the ticket tiers tab (to another tab or closes the editor), check if there are unsaved changes. If yes, show a confirmation dialog: 'You have unsaved ticket tier changes. Do you want to save them before leaving?' with options to Save, Discard, or Cancel.

Wire the PATCH /api/organizer/events/:eventId endpoint so that when the organizer saves ticket tiers, the request includes both event details (if they edited those) and the ticket_tiers array. The backend should handle creating, updating, and deleting tiers as needed in a single transaction.

After a successful save, show a toast notification: 'Ticket tiers updated successfully'. If the save fails, show an error toast with the error message and keep the form open so the user can retry.

Make sure the eventId is passed correctly from the URL or parent component to the ticket tier management page.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the ticket tier feature is properly integrated into the event editor.

Confirm that:
- [ ] The event editor has a 'Ticket Tiers' tab
- [ ] Clicking the tab loads the ticket tier management interface
- [ ] The tab shows a badge with the tier count
- [ ] Navigating away with unsaved changes shows a confirmation dialog
- [ ] Clicking 'Save' in the dialog saves the tiers
- [ ] Clicking 'Discard' abandons changes and navigates away
- [ ] Clicking 'Cancel' stays on the ticket tiers tab
- [ ] After saving, a success toast appears
- [ ] On error, an error toast appears and the form stays open
- [ ] The eventId is correctly passed to the ticket tier component

Test the actual flow: Open an event editor → click the 'Ticket Tiers' tab → add a new tier → click another tab → confirmation dialog appears → click 'Save' → success toast → tiers are saved → refresh the page → tiers are still there. Try again: add a tier → click another tab → click 'Discard' → navigate away without saving → refresh → the new tier is gone.

Then give me your honest advice on:
- Whether the unsaved changes warning is clear and helpful
- Any navigation edge cases (back button, browser close, etc.)
- How this integrates with the rest of the event editor (does saving tiers also save event details?)
- Whether the UX flow feels natural
- What could be confusing to an organizer
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: