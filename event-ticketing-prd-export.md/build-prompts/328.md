# Step 328 of 345: Test the fraud detection feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_fraud-detection-prevention

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the fraud detection feature doesn't break and works correctly.

**Unit Tests:**
- Error handler: maps API error codes to user-friendly messages (401 → 'Session expired', 403 → 'Access Denied', 429 → 'Too many requests', etc.)
- Risk badge component: renders correct color for risk levels (green for low 0-30, amber for medium 31-70, red for high 71-100)
- Date range filter: validates start/end dates, rejects invalid ranges (end before start)

**Component/Widget Tests:**
- FraudMetricCard: displays the metric value, label, and trend indicator (up/down arrow)
- FraudTransactionTable: renders rows with order ID, amount, email, timestamp, risk score, status; sorting works by clicking headers; filtering works
- RiskBadge: shows correct color and label for each risk level
- FraudTransactionReviewModal: displays order summary, buyer info, payment details, fraud analysis; approve/reject buttons work; confirmation dialog appears

**Integration Tests (Full User Flows):**
- Admin flow: Log in as admin → navigate to fraud dashboard → see list of flagged transactions → filter by high risk → see only high-risk transactions → click a transaction → modal opens with details → click 'Approve' → confirmation dialog → confirm → modal closes and dashboard updates → transaction status changes to 'approved'
- Checkout flow: Start checkout → simulate medium-risk transaction → see 'Additional verification required' message → payment button disabled → simulate high-risk transaction → see 'Unable to process payment' error → contact support link works
- Real-time updates: Open fraud dashboard → toggle real-time updates on → wait 30 seconds → new transactions appear (if any) → toggle off → polling stops
- CSV export: Open fraud dashboard → filter transactions → click 'Export as CSV' → file downloads with correct data
- Error handling: Open dashboard with no connection → see error → turn connection on → retry → transactions load. Let session expire → try any action → redirected to login.

**API Tests:**
- GET /api/admin/fraud/flagged-transactions: returns paginated list with correct filters, sorting, and pagination
- GET /api/admin/fraud/transaction-details/:fraudEventId: returns complete fraud analysis for a transaction
- POST /api/admin/fraud/review-transaction: accepts approve/reject decision with optional notes, updates fraud_events status, logs to auditLogs
- All endpoints reject requests without auth (401), reject non-admins (403), and enforce rate limits (429)

**Test Data:**
- Create mock fraud events with different risk levels (low, medium, high)
- Create mock orders and payments associated with fraud events
- Create mock users (admin and non-admin) for permission testing

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: