# Step 338 of 345: Test the delivery feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_ticket-delivery-distribution

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the delivery feature doesn't break and works as expected.

Unit tests:
- useTicketDeliveryStatus hook: fetches delivery status correctly, resendTicket() calls the right endpoint with correct params, handles errors
- useDeliveryPreferences hook: fetches preferences correctly, updatePreferences() sends only changed fields, verifyEmail() and verifyPhone() call the right endpoints
- useAdminDeliveryEvents hook: fetches delivery events, filters work, search is debounced, polling refreshes every 30 seconds

Component tests (React Testing Library):
- TicketStatusPage: renders status badge with correct color, QR code displays, delivery history timeline shows all attempts, resend button works and shows spinner, error states render correctly
- DeliverySettingsPage: all toggle switches work, email/phone inputs accept changes, language dropdown works, save button persists changes, verification status indicators show correctly
- AdminDeliveryDashboardPage: metric cards display correct numbers, delivery table renders all columns, filters work, search works, expandable rows show details, bulk actions work

Integration tests (full user flows):
- User views their ticket status → sees delivery status and history → clicks resend → sees success message → delivery history updates
- User opens delivery settings → changes email → sees verification prompt → verifies email → sees 'Verified' status → saves preferences → sees success message
- Admin opens delivery dashboard → sees metrics and delivery table → filters by status → results update → searches by ticket ID → results narrow → clicks retry failed → sees confirmation

API endpoint tests:
- GET /api/tickets/:ticketId/delivery-status: returns correct delivery status with valid auth, rejects without auth, returns 403 for other users' tickets, returns 404 for non-existent tickets
- POST /api/tickets/:ticketId/resend-delivery: creates new delivery attempt with valid auth, rejects without auth, returns 403 for other users' tickets, respects rate limits, validates email/phone format
- GET /api/admin/delivery/events: returns all delivery events with valid admin auth, rejects without auth, returns 403 for non-admins, filters work, search works
- POST /api/admin/delivery/retry: retries failed deliveries with valid admin auth, rejects without auth, returns 403 for non-adm

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: