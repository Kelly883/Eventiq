# Step 124 of 345: Set up routes for QR code ticketing pages

================================================================================

**Category:** Navigation & Routing
**Flow:** feature_qr-code-ticket-generation-verification

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the routing for the QR code ticketing feature in the React app.

Add a new route for VenueCheckInPage at /venue/check-in/:eventId. This page should be protected — only users with 'VenueStaff' or 'Organizer' role can access it. If a user without the right role tries to access it, redirect to a 'Not Authorized' page or back to the home page.

The :eventId parameter should be extracted from the URL and passed to the page as a prop. The page will use this to load event details and sync check-ins.

Set up a route guard or middleware that checks the user's role before allowing access to /venue/check-in/*. This should happen on the frontend (for UX) and also be enforced on the backend (for security) via the Policy checks in the API endpoints.

If the user is not authenticated at all, redirect to the login page. If authenticated but without the right role, show a 'You don't have permission to access this page' message.

Consider adding a breadcrumb or back button on VenueCheckInPage so staff can navigate back to an event list or dashboard.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the routing is set up correctly and protected.

Confirm that:
- [ ] VenueCheckInPage is accessible at /venue/check-in/:eventId when authenticated
- [ ] The eventId parameter is correctly extracted and passed to the page
- [ ] Unauthenticated users are redirected to login
- [ ] Users without 'VenueStaff' or 'Organizer' role see a 'Not Authorized' message
- [ ] The route guard works on the frontend
- [ ] The page has a back button or breadcrumb for navigation
- [ ] Deep linking to /venue/check-in/:eventId works correctly

Test the actual flow: While logged out, try accessing /venue/check-in/test-event-id → redirected to login. Log in as a regular user → try accessing the same URL → see 'Not Authorized'. Log in as venue staff → access the URL → page loads. Try accessing with an invalid eventId → see appropriate error.

Then give me your honest advice on:
- Whether the route protection is strong enough
- Any edge cases in the routing (invalid eventId, deleted event)
- How the navigation feels for venue staff (is it easy to get back?)
- Whether we need additional routes (event list, staff dashboard)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: