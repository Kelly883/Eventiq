# Step 329 of 345: Test the email template management feature

================================================================================

**Category:** Testing
**Flow:** feature_email-notification-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the email template feature works correctly and doesn't break.

Unit tests:
- useEmailTemplates hook: test that selectTemplate(id) fetches the correct template, saveTemplate(id, data) calls PATCH with the right payload, deleteTemplate(id) calls DELETE and removes from list, createTemplate(data) calls POST and adds to list, sendTestEmail(templateId, email) calls POST /api/admin/email-templates/send-test, refetch() reloads the list.
- Error handler utility: test that network errors are mapped to user-friendly messages, 401 errors trigger redirect, 403 errors show access denied, validation errors are extracted and displayed.

Component tests:
- AdminEmailTemplateManagementPage: test that the template list renders, clicking a template loads its details, the editor form shows name/subject/body fields, clicking Save calls saveTemplate, clicking Delete shows confirmation then calls deleteTemplate, clicking 'Send Test Email' opens modal and calls sendTestEmail, loading state shows skeletons, empty state shows CTA, error state shows banner with retry.
- EmailTemplateEditor: test that TipTap editor renders, placeholder variables can be inserted, form validation works (empty name/subject/body shows errors).
- SendTestEmailModal: test that modal opens, user can enter email address, clicking Send calls sendTestEmail, success shows confirmation, failure shows error.

Integration tests:
- Full flow: Load the page as admin → see template list → click a template → editor populates → edit subject → insert placeholder variable → click Save → see success message → template list updates → click 'Send Test Email' → enter test email → test email is sent → click Delete → confirmation dialog → delete succeeds → template disappears from list → click 'Create New Template' → fill form → create → new template appears in list.
- Error flow: Load page → network error → click Retry → page loads. Edit template → save fails → notification shows → click Retry → save succeeds. Try accessing page as non-admin → redirected to 403.

API tests (backend):
- GET /api/admin/email-templates: returns list of templates for admin, returns 403 for non-admin, returns 401 for unauthenticated.
- GET /api/admin/email-templates/:templateId: returns template details for admin, returns 403 for non-admin.
- POST /api/admin/email-templates: creates template with valid data, returns validation errors for invalid data, returns 403 for non-admin.
- PATCH /api/admin/email-templates/:templateId: updates template, sanitizes HTML content, returns validation errors for invalid data.
- DELETE /api/admin/email-templates/:templateId: deletes template, returns 403 for non-admin.
- POST /api/admin/email-templates/send-test: sends test email, rate-limits to 5 per minute, returns 403 for non-admin.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (hooks, utilities)
- [ ] All component tests pass (page, editor, modal)
- [ ] All integration tests pass on a real device/environment
- [ ] All API tests pass (all six endpoints tested)
- [ ] Test coverage is decent (hooks, components, and API endpoints should be well-covered)
- [ ] No flaky tests (tests pass consistently)

Test the actual flow: Run the full test suite. Then run the integration test: load the page as admin → see template list → click a template → editor populates → edit and save → success → template list updates → send test email → delete template → create new template → all operations succeed. Try as non-admin → redirected to 403. Test error scenarios: network failure → retry succeeds, save failure → retry succeeds, validation errors → shown inline.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Tests you'd add before feeling confident about a release
- Whether the test coverage is sufficient for a feature that manages email templates
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: