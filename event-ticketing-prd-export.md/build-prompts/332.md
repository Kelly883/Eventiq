# Step 332 of 345: Test the organizer profile feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_organizer-profile-branding

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the organizer profile feature doesn't break.

Unit tests:
- Organizer data validation: displayName required, bio max 500 chars, URLs valid format, hex colors valid
- Privacy filtering: email/phone only included if emailPublic/phonePublic are true
- Branding color application: primaryColor and accentColor are correctly applied to UI elements

Component/Widget tests:
- OrganizerPublicProfilePage: loading state shows skeleton, error state shows retry button, profile data displays correctly, event cards render in grid
- OrganizerProfileEditPage: form fields populate with current data, validation errors highlight fields, save button shows loading spinner, unsaved changes warning appears on exit
- OrganizerProfileSettingsPage: stat cards display correctly, toggles work, audit log shows changes (admin only)

Integration tests (full user flows):
- Public profile view: navigate to /organizer/:organizerId → profile loads → see avatar, name, bio, contact info (if public), social links, event cards → filter events by category → see filtered results
- Edit profile: navigate to /organizer/profile/edit → form populates → edit displayName → change branding colors → see live preview → upload avatar → save → redirected to public profile → see changes reflected
- Settings: navigate to /organizer/profile/settings → see stats → toggle notification preference → save → see confirmation toast → toggle profile visibility → save → verify public profile is now private (returns 404)
- Error handling: try accessing edit page while logged out → redirected to login → log in → redirected back to edit page. Try uploading invalid file → see error. Try saving with empty displayName → see validation error.

API endpoint tests:
- GET /api/organizers/:organizerId: returns public data only, filters sensitive fields based on privacy toggles, returns 404 for private profiles when not owner
- GET /api/organizers/:organizerId/events: returns events for public profiles, filters by status, paginates correctly
- GET /api/organizers/me: requires auth, requires Organizer role, returns all profile data including private fields
- PATCH /api/organizers/me/update: validates inputs, updates profile, logs changes to auditLogs, enforces rate limit
- POST /api/organizers/me/upload-avatar: validates file type and size, uploads to storage, updates avatarUrl, deletes old avatar

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (validation, privacy filtering, branding)
- [ ] All component tests pass (pages load, forms work, errors display)
- [ ] All integration tests pass on a real device
- [ ] All API endpoint tests pass (auth, validation, rate limits)
- [ ] Test coverage is decent (pages, state management, validation, error handling)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: navigate to a public organizer profile → see all data → filter events → go to edit page → edit profile → save → see changes on public profile. Go to settings → toggle notification preference → see confirmation. Try accessing edit page while logged out → redirected to login → log in → redirected back to edit page. Try uploading invalid file → see error. All interactions should feel smooth with no errors.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Whether the API tests adequately cover edge cases (rate limits, validation, auth)
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: