# Calendar Endpoint List By Date

GET /api/events/public/calendar

## Description

REQUEST: Query params: ?month=2024-03 (ISO format YYYY-MM, optional), ?week=2024-W12 (ISO week format, optional), ?category=concerts (optional), ?priceMin=0&priceMax=500 (optional), ?location=New York (optional)
PROCESS: 1. Validate query params (valid month/week format, price range valid) 2. Query events collection filtered by status='published' 3. Apply date range filter based on month or week param 4. Apply category, price, and location filters if provided 5. Join with pricing_windows to get current prices and availability 6. Join with ticket_inventory to get stock levels 7. Group events by date 8. Return calendar-formatted data with event count per date and color-coded availability
SUCCESS (200): { dates: [{date: ISO8601, eventCount: number, availability: 'available'|'low'|'sold_out', events: [{id, title, organizer: {name, avatar}, startTime, venue, startingPrice, ticketsAvailable}]}], month: string, week: string }
ERRORS: 400 (invalid month/week/price format), 500 (database error)
SECURITY: Public endpoint (no auth required). Rate limit 30/min per IP to prevent scraping. Only returns published events.

---
Generated by VisualPRD