# Dashboard Endpoint Update Preferences

PATCH /api/users/me/dashboard-preferences

## Description

REQUEST: { defaultTicketFilter (string, optional - must be one of: all/upcoming/past), defaultDateRange (string, optional - must be one of: 7days/30days/90days/all), showRecommendations (boolean, optional), showActivityFeed (boolean, optional), autoRefreshEnabled (boolean, optional) }
PROCESS: 1. Verify auth token and extract userId 2. Validate input (if provided, defaultTicketFilter must be valid, defaultDateRange must be valid) 3. Query user_dashboard_preferences by userId 4. If not found, create new record with defaults 5. Update provided fields 6. Return updated preferences
SUCCESS (200): { defaultTicketFilter, defaultDateRange, showRecommendations, showActivityFeed, autoRefreshEnabled }
ERRORS: 400 (validation failed - invalid filter, invalid dateRange), 401 (user not authenticated), 403 (user does not have permission), 500 (database error)
SECURITY: Auth required (bearer token). User can only update their own preferences (enforced by Policy). Rate limit 10/min per user to prevent spam.

---
Generated by VisualPRD