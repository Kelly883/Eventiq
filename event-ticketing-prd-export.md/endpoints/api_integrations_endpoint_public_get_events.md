# Api Integrations Endpoint Public Get Events

GET /v1/events

## Description

Public API endpoint to list events for the organizer associated with the provided API key. The API key must have the 'events:read' scope. Uses a dedicated middleware for API key authentication. Eager loads relationships and uses API Resources for consistent output.

---
Generated by VisualPRD