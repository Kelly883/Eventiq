# Localization Endpoint Update Preferences

PATCH /api/users/me/language-preferences/update

## Description

REQUEST: { language (string, optional, ISO 639-1 code), region (string, optional, ISO 3166-1 code), dateFormat (string, optional, 'MM/DD/YYYY'|'DD/MM/YYYY'|'YYYY-MM-DD'), timeFormat (string, optional, '12-hour'|'24-hour'), currency (string, optional, ISO 4217 code), numberFormat (string, optional, 'comma'|'period'), rtlEnabled (boolean, optional) }. PROCESS: 1. Verify auth token and extract userId 2. Validate language code is supported (check against i18next config) 3. Validate dateFormat, timeFormat, numberFormat are valid options 4. Query localization_preferences by userId 5. If not found, create new record with defaults 6. Update only provided fields 7. Set updatedAt to current timestamp 8. Create auditLog entry: action='language_preferences_updated', userId, changedFields 9. Return updated preferences. SUCCESS (200): { language, region, dateFormat, timeFormat, currency, numberFormat, rtlEnabled, message: 'Language preferences updated' }. ERRORS: 400 (validation error - unsupported language, invalid format), 401 (unauthenticated), 403 (user doesn't have permission), 429 (rate limited), 500 (database error). SECURITY: Auth required (bearer token). User can only update their own preferences (enforced by Policy). Rate limit 5 language changes/min per user to prevent spam. All changes logged to auditLogs.

---
Generated by VisualPRD