# Fraud Endpoint Admin Flagged

GET /api/admin/fraud/flagged-transactions

## Description

REQUEST: Query params: ?page=1&limit=50&riskLevel=high|medium|low&status=flagged|reviewed|approved|rejected&startDate=ISO8601&endDate=ISO8601&sortBy=riskScore|createdAt&sortOrder=asc|desc
PROCESS: 1. Verify auth token and extract userId 2. Check user has 'Admin' role via Policy 3. Query fraud_events filtered by status (default: 'flagged'), riskLevel, date range 4. Eager load related orders, users, payments for display 5. Sort by requested field 6. Paginate with limit/offset 7. Return paginated list with order details and fraud metrics
SUCCESS (200): { transactions: [{id, orderId, userId, email, amount, riskScore, riskLevel, detectionMethod, fraudFactors, status, createdAt, reviewedBy, reviewedAt}], total, page, limit }
ERRORS: 400 (invalid query params), 401 (unauthenticated), 403 (user not admin), 500 (database error)
SECURITY: Admin-only (requires 'Admin' role via Policy). Auth required (bearer token). Rate limit 20/min per user to prevent abuse.

---
Generated by VisualPRD