# permissionRequests

## Description

PURPOSE: Track user requests for elevated permissions

FIELDS:
id: string
├── Purpose: UUID primary key
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key to users.id — user requesting permission
│   ├── Constraints: required
│   └── Mutable: no

permissionId: string
├── Purpose: Foreign key to permissions.id — permission being requested
│   ├── Constraints: required
│   └── Mutable: no

status: string
├── Purpose: Request status: pending, approved, denied
│   ├── Constraints: required
│   └── Mutable: yes

reason: string
├── Purpose: Optional reason provided by user for requesting permission
│   └── Mutable: yes

approvedBy: string
├── Purpose: Foreign key to users.id — admin who approved/denied the request
│   └── Mutable: yes

approvalReason: string
├── Purpose: Optional reason provided by admin for approval/denial
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Request creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

resolvedAt: timestamp
└── Purpose: Timestamp when request was approved or denied
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key |
| userId | string | Yes | Foreign key to users.id — user requesting permission |
| permissionId | string | Yes | Foreign key to permissions.id — permission being requested |
| status | string | Yes | Request status: pending, approved, denied |
| reason | string | No | Optional reason provided by user for requesting permission |
| approvedBy | string | No | Foreign key to users.id — admin who approved/denied the request |
| approvalReason | string | No | Optional reason provided by admin for approval/denial |
| createdAt | timestamp | Yes | Request creation timestamp |
| resolvedAt | timestamp | No | Timestamp when request was approved or denied |

---
Generated by VisualPRD