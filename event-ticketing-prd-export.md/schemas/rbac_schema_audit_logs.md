# auditLogs

## Description

PURPOSE: Immutable audit trail of all role and permission changes

FIELDS:
id: string
├── Purpose: UUID primary key
│   ├── Constraints: required
│   └── Mutable: no

adminId: string
├── Purpose: Foreign key to users.id — admin who made the change
│   ├── Constraints: required
│   └── Mutable: no

targetUserId: string
├── Purpose: Foreign key to users.id — user whose role/permissions changed
│   ├── Constraints: required
│   └── Mutable: no

action: string
├── Purpose: Action type: role_assigned, role_revoked, permission_granted, permission_revoked, permission_request_approved, permission_request_denied
│   ├── Constraints: required
│   └── Mutable: yes

oldValue: object
├── Purpose: Previous role/permission state (e.g., {role: 'Attendee', permissions: [...]})
│   └── Mutable: yes

newValue: object
├── Purpose: New role/permission state (e.g., {role: 'Organizer', permissions: [...]})
│   └── Mutable: yes

reason: string
├── Purpose: Optional reason provided by admin for the change
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Timestamp of the change
    ├── Constraints: required
    └── Mutable: no

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key |
| adminId | string | Yes | Foreign key to users.id — admin who made the change |
| targetUserId | string | Yes | Foreign key to users.id — user whose role/permissions changed |
| action | string | Yes | Action type: role_assigned, role_revoked, permission_granted, permission_revoked, permission_request_approved, permission_request_denied |
| oldValue | object | No | Previous role/permission state (e.g., {role: 'Attendee', permissions: [...]}) |
| newValue | object | No | New role/permission state (e.g., {role: 'Organizer', permissions: [...]}) |
| reason | string | No | Optional reason provided by admin for the change |
| createdAt | timestamp | Yes | Timestamp of the change |

---
Generated by VisualPRD