# permissions

## Description

PURPOSE: Define all available permissions in the system

FIELDS:
id: string
├── Purpose: Unique permission identifier (e.g., 'can_create_events')
│   ├── Constraints: required
│   └── Mutable: no

name: string
├── Purpose: Human-readable permission name (e.g., 'Create Events')
│   ├── Constraints: required
│   └── Mutable: yes

description: string
├── Purpose: Detailed description of what this permission allows
│   ├── Constraints: required
│   └── Mutable: yes

category: string
├── Purpose: Permission category: event_management, ticket_management, analytics, user_management, platform_admin
│   ├── Constraints: required
│   └── Mutable: yes

riskLevel: string
├── Purpose: Risk level: low, medium, high (high-risk permissions require admin approval)
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Permission definition creation timestamp
    ├── Constraints: required
    └── Mutable: no

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | Unique permission identifier (e.g., 'can_create_events') |
| name | string | Yes | Human-readable permission name (e.g., 'Create Events') |
| description | string | Yes | Detailed description of what this permission allows |
| category | string | Yes | Permission category: event_management, ticket_management, analytics, user_management, platform_admin |
| riskLevel | string | Yes | Risk level: low, medium, high (high-risk permissions require admin approval) |
| createdAt | timestamp | Yes | Permission definition creation timestamp |

---
Generated by VisualPRD