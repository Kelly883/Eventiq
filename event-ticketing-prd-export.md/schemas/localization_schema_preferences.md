# localization_preferences

## Description

PURPOSE: Store user language and locale preferences per user, enabling multi-language support with locale-specific formatting (dates, times, numbers, currency)

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship
│   ├── Constraints: required
│   └── Mutable: no

language: string
├── Purpose: ISO 639-1 language code (e.g., 'en', 'es', 'fr', 'de', 'pt', 'zh', 'ja', 'ar'), defaults to 'en'
│   ├── Constraints: required
│   └── Mutable: yes

region: string
├── Purpose: ISO 3166-1 alpha-2 region code (e.g., 'US', 'ES', 'FR'), used for locale-specific formatting
│   └── Mutable: yes

dateFormat: string
├── Purpose: Date format preference: 'MM/DD/YYYY', 'DD/MM/YYYY', 'YYYY-MM-DD', defaults to 'MM/DD/YYYY'
│   ├── Constraints: required
│   └── Mutable: yes

timeFormat: string
├── Purpose: Time format preference: '12-hour' or '24-hour', defaults to '12-hour'
│   ├── Constraints: required
│   └── Mutable: yes

currency: string
├── Purpose: ISO 4217 currency code (e.g., 'USD', 'EUR', 'GBP'), used for price display
│   └── Mutable: yes

numberFormat: string
├── Purpose: Number formatting preference: 'comma' (1,234.56) or 'period' (1.234,56), defaults to 'comma'
│   ├── Constraints: required
│   └── Mutable: yes

rtlEnabled: boolean
├── Purpose: Whether to enable right-to-left (RTL) layout for Arabic, Hebrew, Urdu, defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Preference creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last preference update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| userId | string | Yes | Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship |
| language | string | Yes | ISO 639-1 language code (e.g., 'en', 'es', 'fr', 'de', 'pt', 'zh', 'ja', 'ar'), defaults to 'en' |
| region | string | No | ISO 3166-1 alpha-2 region code (e.g., 'US', 'ES', 'FR'), used for locale-specific formatting |
| dateFormat | string | Yes | Date format preference: 'MM/DD/YYYY', 'DD/MM/YYYY', 'YYYY-MM-DD', defaults to 'MM/DD/YYYY' |
| timeFormat | string | Yes | Time format preference: '12-hour' or '24-hour', defaults to '12-hour' |
| currency | string | No | ISO 4217 currency code (e.g., 'USD', 'EUR', 'GBP'), used for price display |
| numberFormat | string | Yes | Number formatting preference: 'comma' (1,234.56) or 'period' (1.234,56), defaults to 'comma' |
| rtlEnabled | boolean | Yes | Whether to enable right-to-left (RTL) layout for Arabic, Hebrew, Urdu, defaults to false |
| createdAt | timestamp | Yes | Preference creation timestamp |
| updatedAt | timestamp | Yes | Last preference update timestamp |

---
Generated by VisualPRD