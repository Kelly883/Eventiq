# analytics_tier_performance

## Description

PURPOSE: Aggregate performance metrics per ticket tier, enabling tier-level analytics and revenue breakdown visualizations

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

eventId: string
├── Purpose: Foreign key reference to events.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

ticketTierId: string
├── Purpose: Foreign key reference to ticket_tiers.id, specifies which tier this performance data is for
│   ├── Constraints: required
│   └── Mutable: no

totalSold: integer
├── Purpose: Total tickets sold for this tier, denormalized from ticket_inventory
│   ├── Constraints: required
│   └── Mutable: yes

totalRevenue: decimal(12, 2)
├── Purpose: Total revenue from this tier across all pricing windows
│   ├── Constraints: required
│   └── Mutable: yes

averagePrice: decimal(10, 2)
├── Purpose: Average price per ticket for this tier
│   ├── Constraints: required
│   └── Mutable: yes

percentageOfTotalSales: decimal(5, 2)
├── Purpose: Percentage of total event sales from this tier
│   ├── Constraints: required
│   └── Mutable: yes

percentageOfTotalRevenue: decimal(5, 2)
├── Purpose: Percentage of total event revenue from this tier
│   ├── Constraints: required
│   └── Mutable: yes

conversionRate: decimal(5, 2)
├── Purpose: Conversion rate for this tier (tickets sold / tier page views)
│   ├── Constraints: required
│   └── Mutable: yes

lastUpdatedAt: timestamp
├── Purpose: Timestamp of last performance update
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Performance record creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last performance update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| eventId | string | Yes | Foreign key reference to events.id, indexed for fast lookups |
| ticketTierId | string | Yes | Foreign key reference to ticket_tiers.id, specifies which tier this performance data is for |
| totalSold | integer | Yes | Total tickets sold for this tier, denormalized from ticket_inventory |
| totalRevenue | decimal(12, 2) | Yes | Total revenue from this tier across all pricing windows |
| averagePrice | decimal(10, 2) | Yes | Average price per ticket for this tier |
| percentageOfTotalSales | decimal(5, 2) | Yes | Percentage of total event sales from this tier |
| percentageOfTotalRevenue | decimal(5, 2) | Yes | Percentage of total event revenue from this tier |
| conversionRate | decimal(5, 2) | Yes | Conversion rate for this tier (tickets sold / tier page views) |
| lastUpdatedAt | timestamp | Yes | Timestamp of last performance update |
| createdAt | timestamp | Yes | Performance record creation timestamp |
| updatedAt | timestamp | Yes | Last performance update timestamp |

---
Generated by VisualPRD