# ticket_inventory

## Description

PURPOSE: Aggregate inventory tracking per ticket tier, denormalizing sold/available counts for fast dashboard queries

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

eventId: string
├── Purpose: Foreign key reference to events.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

ticketTierId: string
├── Purpose: Foreign key reference to ticket_tiers.id, specifies which ticket category this inventory tracks
│   ├── Constraints: required
│   └── Mutable: no

totalAllocated: integer
├── Purpose: Total tickets allocated for this tier across all pricing windows, denormalized from sum of pricing_windows.quantityLimit
│   ├── Constraints: required
│   └── Mutable: yes

totalSold: integer
├── Purpose: Denormalized count of tickets sold for this tier across all pricing windows, updated atomically on each purchase
│   ├── Constraints: required
│   └── Mutable: yes

totalAvailable: integer
├── Purpose: Computed field: totalAllocated - totalSold, represents remaining inventory for this tier
│   ├── Constraints: required
│   └── Mutable: yes

lowStockThreshold: integer
├── Purpose: Optional threshold (e.g., 10 tickets) below which low-stock alerts trigger, configurable per tier
│   └── Mutable: yes

isLowStock: boolean
├── Purpose: Computed flag: true if totalAvailable <= lowStockThreshold, used for UI alerts
│   ├── Constraints: required
│   └── Mutable: yes

lastUpdatedAt: timestamp
├── Purpose: Timestamp of last inventory change (purchase, manual adjustment, or allocation update)
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Inventory record creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last inventory update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| eventId | string | Yes | Foreign key reference to events.id, indexed for fast lookups |
| ticketTierId | string | Yes | Foreign key reference to ticket_tiers.id, specifies which ticket category this inventory tracks |
| totalAllocated | integer | Yes | Total tickets allocated for this tier across all pricing windows, denormalized from sum of pricing_windows.quantityLimit |
| totalSold | integer | Yes | Denormalized count of tickets sold for this tier across all pricing windows, updated atomically on each purchase |
| totalAvailable | integer | Yes | Computed field: totalAllocated - totalSold, represents remaining inventory for this tier |
| lowStockThreshold | integer | No | Optional threshold (e.g., 10 tickets) below which low-stock alerts trigger, configurable per tier |
| isLowStock | boolean | Yes | Computed flag: true if totalAvailable <= lowStockThreshold, used for UI alerts |
| lastUpdatedAt | timestamp | Yes | Timestamp of last inventory change (purchase, manual adjustment, or allocation update) |
| createdAt | timestamp | Yes | Inventory record creation timestamp |
| updatedAt | timestamp | Yes | Last inventory update timestamp |

---
Generated by VisualPRD