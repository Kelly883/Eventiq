# analytics_sales_timeline

## Description

PURPOSE: Immutable time-series log of all ticket sales, enabling sales velocity calculations, trend analysis, and detailed transaction history

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

eventId: string
├── Purpose: Foreign key reference to events.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

ticketTierId: string
├── Purpose: Foreign key reference to ticket_tiers.id, specifies which tier this sale is for
│   ├── Constraints: required
│   └── Mutable: no

pricingWindowId: string
├── Purpose: Foreign key reference to pricing_windows.id, specifies which pricing window this sale occurred in
│   └── Mutable: no

saleTimestamp: timestamp
├── Purpose: Exact timestamp of ticket sale, indexed for time-series queries
│   ├── Constraints: required
│   └── Mutable: yes

quantity: integer
├── Purpose: Number of tickets sold in this transaction
│   ├── Constraints: required
│   └── Mutable: yes

unitPrice: decimal(10, 2)
├── Purpose: Price per ticket at time of sale
│   ├── Constraints: required
│   └── Mutable: yes

totalAmount: decimal(12, 2)
├── Purpose: Total revenue from this transaction (quantity * unitPrice)
│   ├── Constraints: required
│   └── Mutable: yes

buyerEmail: string
├── Purpose: Buyer email address (if available), hashed for privacy
│   └── Mutable: yes

source: string
├── Purpose: Sales source: 'direct', 'affiliate', 'social', 'email', 'organic', tracked via Mixpanel
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Record creation timestamp
    ├── Constraints: required
    └── Mutable: no

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| eventId | string | Yes | Foreign key reference to events.id, indexed for fast lookups |
| ticketTierId | string | Yes | Foreign key reference to ticket_tiers.id, specifies which tier this sale is for |
| pricingWindowId | string | No | Foreign key reference to pricing_windows.id, specifies which pricing window this sale occurred in |
| saleTimestamp | timestamp | Yes | Exact timestamp of ticket sale, indexed for time-series queries |
| quantity | integer | Yes | Number of tickets sold in this transaction |
| unitPrice | decimal(10, 2) | Yes | Price per ticket at time of sale |
| totalAmount | decimal(12, 2) | Yes | Total revenue from this transaction (quantity * unitPrice) |
| buyerEmail | string | No | Buyer email address (if available), hashed for privacy |
| source | string | No | Sales source: 'direct', 'affiliate', 'social', 'email', 'organic', tracked via Mixpanel |
| createdAt | timestamp | Yes | Record creation timestamp |

---
Generated by VisualPRD