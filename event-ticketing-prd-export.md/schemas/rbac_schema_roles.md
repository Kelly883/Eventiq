# roles

## Description

PURPOSE: Define roles and their associated permissions

FIELDS:
id: string
├── Purpose: UUID primary key
│   ├── Constraints: required
│   └── Mutable: no

name: string
├── Purpose: Role name: Admin, Organizer, Attendee, Support Staff
│   ├── Constraints: required
│   └── Mutable: yes

description: string
├── Purpose: Human-readable description of role purpose
│   ├── Constraints: required
│   └── Mutable: yes

permissions: array<string>
├── Purpose: Array of permission IDs granted to this role (e.g., ['can_create_events', 'can_manage_tickets'])
│   ├── Constraints: required
│   └── Mutable: yes

isSystemRole: boolean
├── Purpose: True for built-in roles (Admin, Organizer, Attendee, Support), false for custom roles
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Role creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last role update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key |
| name | string | Yes | Role name: Admin, Organizer, Attendee, Support Staff |
| description | string | Yes | Human-readable description of role purpose |
| permissions | array<string> | Yes | Array of permission IDs granted to this role (e.g., ['can_create_events', 'can_manage_tickets']) |
| isSystemRole | boolean | Yes | True for built-in roles (Admin, Organizer, Attendee, Support), false for custom roles |
| createdAt | timestamp | Yes | Role creation timestamp |
| updatedAt | timestamp | Yes | Last role update timestamp |

---
Generated by VisualPRD