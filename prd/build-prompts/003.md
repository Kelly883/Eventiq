# Step 3 of 345: Set up organizer profile feature folder structure

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_organizer-profile-branding

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an organizer profile and branding system. For this step, I need you to set up the folder structure for the organizer profile feature.

Create a feature folder at src/features/organizer-profile/ with these subfolders:
- pages/ (we'll have OrganizerPublicProfilePage, OrganizerProfileEditPage, and OrganizerProfileSettingsPage)
- components/ (reusable UI pieces like ProfileHeader, BrandingColorPicker, EventCard, PrivacyToggle, AuditLogViewer)
- hooks/ (custom React hooks for fetching and managing organizer data)
- types/ (TypeScript interfaces for organizer data)
- services/ (API client functions for organizer endpoints)
- utils/ (helper functions like color validation, privacy filtering)

On the backend side, create a Laravel folder structure at app/Http/Controllers/OrganizerController.php, app/Models/Organizer.php, app/Policies/OrganizerPolicy.php, and app/Http/Requests/UpdateOrganizerRequest.php.

This feature has three pages and handles sensitive profile data, so keeping components, hooks, and services separate will make the code maintainable and testable.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The organizer-profile feature folder exists with all subfolders (pages, components, hooks, types, services, utils)
- [ ] The backend Laravel files exist (OrganizerController, Organizer model, OrganizerPolicy, UpdateOrganizerRequest)
- [ ] The project still builds without errors after adding these folders
- [ ] No TypeScript or build errors appear in the console

Test the actual flow: Just run a build and make sure nothing is broken — this is just scaffolding.

Then give me your honest advice on:
- Whether this folder structure makes sense for the number of files we'll have
- If there's a better way to organize things given the project's existing structure
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: