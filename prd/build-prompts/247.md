# Step 247 of 345: Wire email templates into the admin settings navigation

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_email-notification-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the AdminEmailTemplateManagementPage to the rest of the admin interface.

Add a route at /admin/settings/email-templates that renders AdminEmailTemplateManagementPage. This route should be protected — only users with the Admin role can access it. If a non-admin tries to access it, redirect to a 403 Forbidden page.

Add a navigation link in the admin settings sidebar or menu that points to /admin/settings/email-templates. Label it 'Email Templates' or 'Manage Email Templates'. This link should only appear for admin users.

Wire up the page so that when the user navigates away (e.g., clicks another admin menu item), any unsaved changes trigger a confirmation dialog: 'You have unsaved changes. Do you want to leave?' — this prevents accidental data loss.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the email templates page is properly wired into the admin interface.

Confirm that:
- [ ] The route /admin/settings/email-templates exists and renders AdminEmailTemplateManagementPage
- [ ] Only users with Admin role can access the page
- [ ] Non-admin users trying to access it are redirected to a 403 page
- [ ] The 'Email Templates' link appears in the admin settings navigation
- [ ] The link only appears for admin users
- [ ] Clicking the link navigates to /admin/settings/email-templates
- [ ] Unsaved changes trigger a confirmation dialog when navigating away
- [ ] Confirming the dialog allows navigation, canceling keeps the user on the page

Test the actual flow: Log in as an admin → navigate to admin settings → see 'Email Templates' link → click it → page loads → edit a template → click another admin menu item → confirmation dialog appears → click 'Cancel' → stay on the page → click 'Leave' → navigate away. Try accessing /admin/settings/email-templates as a non-admin → redirected to 403.

Then give me your honest advice on:
- Whether the navigation placement makes sense for admins
- How the unsaved changes warning feels (too aggressive, not aggressive enough?)
- Any other admin pages that should link to or reference email templates
- What a first-time admin would find confusing about accessing this feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: