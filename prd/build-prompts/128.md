# Step 128 of 345: Set up routing for refund pages

================================================================================

**Category:** Navigation & Routing
**Flow:** feature_refund-cancellation-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up React Router navigation so users can access the three refund pages: UserRefundRequestPage, UserRefundStatusPage, and AdminRefundDashboardPage.

In your React Router configuration (likely in src/App.tsx or src/router.tsx), add these routes:

1. /my-tickets/:ticketId/refund-request → UserRefundRequestPage
   - This page allows authenticated users to request a refund for a specific ticket
   - Requires auth (redirect to login if not authenticated)
   - Requires that the user owns the ticket (enforced by the backend, but show 403 error if not)

2. /my-tickets/:ticketId/refund-status → UserRef

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: