# Step 337 of 345: Test the checkout and payment feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_secure-checkout-payment-processing

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the checkout flow doesn't break.

Unit tests — Test the error mapper: each Stripe error code maps to the right user message. Test the cart context: adding/removing items, updating quantities, persisting to local storage. Test the checkout context: step transitions, billing info storage, client secret management.

Component tests — CartPage: displays items, quantity controls work, 'Proceed to Checkout' button is disabled when empty. CheckoutPage: Step 1 shows order summary and calls POST /api/cart/verify, Step 2 collects billing info, Step 3 displays Stripe CardElement. OrderConfirmationPage: displays order details, 'View My Tickets' button navigates correctly. UserTicketsPage: displays tickets grouped by event, QR codes render, empty state shows.

Integration tests (full user flows) — Complete purchase flow: add items to cart → view CartPage → proceed to checkout → verify cart → enter billing info → enter payment info (use Stripe test card 4242 4242 4242 4242) → submit payment → see OrderConfirmationPage with correct order details. View tickets: from OrderConfirmationPage, click 'View My Tickets' → UserTicketsPage shows the purchased tickets with QR codes. Error handling: start checkout → turn off network → see error → turn network back on → retry succeeds. Session expiration: let session expire during checkout → redirected to login → log back in → can access order confirmation.

API endpoint tests — POST /api/cart/verify: valid cart returns 200, invalid items return 400, unauthenticated returns 401. POST /api/checkout/create-payment-intent: creates pending order, returns client secret, subsequent calls return new secret. GET /api/orders/:orderId: returns order for owner, 403 for non-owner, 404 for non-existent. GET /api/my-tickets: returns user's tickets, 401 without auth. POST /api/webhooks/payment-provider: valid Stripe webhook processes payment, invalid signature rejected.

Security tests — Try accessing another user's order → 403. Try POST /api/checkout/create-payment-intent without auth → 401. Send fake webhook → rejected. Modify cart total on client → server recalculates correctly. Create payment intent twice → second one invalidates first.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (error mapper, contexts)
- [ ] All component tests pass (CartPage, CheckoutPage, OrderConfirmationPage, UserTicketsPage)
- [ ] All integration tests pass on a real device
- [ ] All API endpoint tests pass
- [ ] All security tests pass (auth, authorization, webhook verification)
- [ ] Test coverage is decent (pages, contexts, endpoints, error handling should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration test on a real device: add items to cart → view CartPage → proceed to checkout → verify cart → enter billing info → enter payment info (Stripe test card) → submit → see OrderConfirmationPage → click 'View My Tickets' → see UserTicketsPage with QR codes. All interactions should feel smooth with no errors.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Whether the Stripe test card flow is realistic enough
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: