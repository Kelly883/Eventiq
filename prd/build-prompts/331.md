# Step 331 of 345: Test the authentication feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_user-authentication-accounts

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the authentication feature doesn't break and works as expected.

Unit tests for error handling: Create a test file for error message mapping. Test that each API error code (400, 401, 409, 429, 500) maps to the correct user-friendly message. Test edge cases like missing error response body.

Unit tests for auth context: Test that the AuthContext properly stores and retrieves user data. Test that login populates the user object and stores the token. Test that logout clears both. Test that the loading state resolves correctly. Test that useAuth hook returns the correct values.

Component tests for LoginPage: Test that the form renders with email, password, and submit button. Test that submitting empty fields shows validation errors. Test that submitting valid credentials calls the login endpoint. Test that successful login redirects to /dashboard. Test that wrong password shows 'Invalid email or password'. Test that rate limit (429) shows the appropriate toast. Test that network errors show a dismissible toast with retry. Test that the 'Forgot password?' link navigates to /forgot-password. Test that the 'Create account' link navigates to /register.

Component tests for RegisterPage: Test that the form renders with name, email, password, and confirm password fields. Test that password strength meter updates as user types. Test that submitting with mismatched passwords shows error. Test that submitting with short password shows error. Test that submitting with duplicate email shows 'already registered' with 'Sign In' link. Test that successful registration redirects to /login with success toast. Test that Terms checkbox is required.

Component tests for ForgotPasswordPage: Test that the form renders with email field. Test that submitting shows success message regardless of email. Test that success state replaces form with mail icon and instructions. Test that rate limit shows appropriate message. Test that 'Back to Sign In' link navigates to /login.

Component tests for ResetPasswordPage: Test that invalid token shows error with 'Request new link' button. Test that valid token reveals password fields. Test that password strength meter works. Test that submitting with short password shows error. Test that successful reset redirects to /login with success toast.

Integration tests (full user flows):
1. Register with new email → redirects to /login → log in with those credentials → redirects to /dashboard → log out → redirects to /login.
2. Request password reset → use token from email → reset password → redirected to /login → log in with new password → success.
3. Try logging in 6 times with wrong password → 6th attempt shows rate limit → wait and retry → success.
4. Try accessing /dashboard while logged out → redirected to /login → log in → land on /dashboard.
5. Deep link to /dashboard while logged out → redirected to /login → log in → land on /dashboard (or originally requested page).

API endpoint tests: Test that POST /api/auth/login returns 200 with token and user info on success, 401 on wrong password, 429 on rate limit, 400 on missing fields. Test that POST

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: