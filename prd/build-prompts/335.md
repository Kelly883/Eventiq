# Step 335 of 345: Test the organizer dashboard end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_organizer-dashboard

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the dashboard doesn't break and all features work as expected.

Unit tests:
- Zustand store (dashboardStore): setSelectedEventId() updates state, setEventFilter() updates state, setDateRange() updates state, toggleAutoRefresh() toggles boolean, store persists to localStorage and loads on init.
- Error handler: maps 401 to 'Session expired', maps 403 to 'Access Denied', maps 500 to 'Something went wrong', maps network error to 'Check your connection'.
- CSV export utility: formats activity data correctly, generates valid CSV with headers, includes all required columns (Event Name, Tier Name, Quantity, Unit Price, Total Amount, Sale Timestamp, Buyer Email).

Component tests (React Testing Library):
- OrganizerDashboardPage: renders four metric cards, renders event grid, renders activity feed, filter dropdown works, date range picker works, 'Create New Event' button is clickable, live indicator shows when auto-refresh is enabled.
- EventDetailWidget: renders event title, date, venue, status, renders tier breakdown with bars, renders pricing timeline with color coding, action buttons are clickable, expands/collapses correctly.
- SalesActivityFeed: renders activity items with event badges, tier names, quantities, prices, timestamps, new items appear at top when feed updates, 'Export Activity' button is clickable, empty state shows 'No sales yet'.
- MetricCard: displays metric value, displays trend indicator (up/down/flat), handles loading state with skeleton, handles error state with 'N/A'.

Integration tests (full user flows):
- Load dashboard → see summary metrics and event grid → filter by 'upcoming' → event list updates → change date range to '7days' → metrics re-fetch → click event card → EventDetailWidget expands → click 'View Analytics' → navigate to analytics page → click back → return to dashboard.
- Load dashboard → toggle auto-refresh on → watch activity feed update every 30 seconds → toggle auto-refresh off → activity feed stops updating → click 'Export Activity' → CSV downloads with correct data.
- Load dashboard → simulate 401 error → redirected to login with 'Session expired' toast → log back in → dashboard loads.
- Load dashboard → simulate 500 error → error banner shows with 'Retry' button → click retry → dashboard reloads successfully.

API endpoint tests:
- GET /api/organizer/dashboard/overview: returns 200 with correct metrics, returns 401 without auth, returns 403 without Organizer role, returns 400 with invalid dateRange, returns 400 with invalid eventFilter.
- GET /api/organizer/dashboard/events: returns 200 with event list, returns 401 without auth, returns 403 without Organizer role, returns 400 with invalid status, pagination works with limit/offset.
- GET /api/organizer/dashboard/events/:eventId: returns 200 with event details, returns 401 without auth, returns 403 if user doesn't own event, returns 404 if event doesn't exist.
- GET /api/organizer/dashboard/activity-feed: returns 200 with activity items, returns 401

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: