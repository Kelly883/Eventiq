# Step 345 of 345: Test the payment feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_payment-gateway-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the payment feature doesn't break. Create unit tests, component tests, and integration tests.

Unit tests:
- Payment methods state: fetching the list sets loading state, adding a method calls the API and refreshes the list, removing a method calls the API and removes it from the list, setting a default updates the flag
- Stripe Connect state: fetching the status sets loading state, initiating connect navigates to the endpoint, returning from Stripe updates the status
- Error handling: Stripe errors are mapped to friendly messages, network errors show appropriate messages

Component tests:
- PaymentMethodsPage: loading state shows skeleton cards, empty state shows 'No payment methods saved', list shows saved cards with brand/last-four/expiration, default card is marked with a badge, 'Add New Card' button opens a modal, 'Remove' button deletes the card, 'Set as Default' button updates the default
- AddPaymentMethodModal: Stripe Elements form renders, invalid card data shows validation errors, submitting with valid data calls stripe.createPaymentMethod, success shows a confirmation and closes the modal
- OrganizerPaymentSettingsPage: loading state shows a spinner, not connected shows 'Connect with Stripe' button, connected shows account ID and dashboard link, non-organizers see 'Access Denied'

Integration tests (full user flows):
- User adds a payment method: open PaymentMethodsPage → click 'Add New Card' → fill in card details → submit → see confirmation toast → new card appears in the list
- User removes a payment method: open PaymentMethodsPage → click 'Remove' on a card → see confirmation toast → card disappears
- User sets a default payment method: open PaymentMethodsPage → click 'Set as Default' on a different card → see the badge move
- Organizer connects Stripe: open OrganizerPaymentSettingsPage → click 'Connect with Stripe' → complete Stripe onboarding → get redirected back → see the connected status

Also test the API endpoints: make sure all payment endpoints return the right responses with valid auth, reject without auth, and handle bad inputs. Test the Stripe webhook endpoint with valid and invalid signatures.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (state management, error handling)
- [ ] All component tests pass (PaymentMethodsPage, AddPaymentMethodModal, OrganizerPaymentSettingsPage)
- [ ] All integration tests pass on a real device
- [ ] API endpoint tests pass (valid auth, no auth, bad inputs)
- [ ] Stripe webhook tests pass (valid signature, invalid signature)
- [ ] Test coverage is decent (pages, state management, and API endpoints should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: open PaymentMethodsPage → add a card → see confirmation → card appears → remove it → see confirmation → card disappears. Open OrganizerPaymentSettingsPage → click 'Connect with Stripe' → complete onboarding → get redirected back → see the connected status. All interactions should feel smooth with no errors.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, Stripe API dependencies, state leaks between tests)
- How to mock Stripe API calls so tests don't depend on real Stripe
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: