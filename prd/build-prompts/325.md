# Step 325 of 345: Test the role and permission management feature

================================================================================

**Category:** Testing
**Flow:** feature_user-role-permission-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the role management feature doesn't break.

Unit tests:
- Error handler: each error code maps to the right user-friendly message
- Role assignment logic: prevents self-demotion, validates roleId, handles empty userIds
- Permission update logic: validates permissionIds, checks riskLevel, prevents self-modification of high-risk permissions

Component tests:
- AdminRoleManagementPage: renders user table, bulk selection works, role assignment modal shows and submits correctly, permission toggles work, audit log loads and displays
- UserPermissionsPage: renders role summary, permission categories are collapsible, 'Request Access' modal works, pending requests display with correct status badges
- RoleConfirmationModal: shows affected user count, displays old/new role, has reason field, submit button calls the API
- PermissionRequestModal: has reason field, submit button calls the API, success shows toast

Integration tests (full user flows):
- Admin flow: log in as admin → navigate to /admin/roles → load user list → select users → change role → confirm → see success toast and table updates → expand a user → toggle permission → confirm → see it update → check audit log shows the changes
- User flow: log in as regular user → navigate to /settings/permissions → see role and permissions → click 'Request Access' → submit reason → see pending request appear with 'pending' status
- Access control: try to access /admin/roles as non-admin → redirected to /settings/permissions with error toast. Try to access while logged out → redirected to login
- Error handling: simulate network failure on user list load → see error banner with retry. Simulate 403 on bulk role assignment → see error modal and redirect. Simulate 401 (expired session) → redirected to login

API endpoint tests:
- GET /api/admin/users/list: returns correct users with valid auth, rejects without auth, rejects non-admin, validates query params
- POST /api/admin/roles/assign: creates audit log entry, invalidates user sessions, prevents self-demotion, uses transaction (rollback on failure)
- POST /api/admin/permissions/update: creates audit log entry, invalidates sessions, requires confirmation for high-risk permissions
- GET /api/admin/audit-log/list: returns immutable logs, rejects non-admin, validates query params

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (error handler, role/permission logic)
- [ ] All component tests pass (AdminRoleManagementPage, UserPermissionsPage, modals)
- [ ] All integration tests pass on a real device
- [ ] All API endpoint tests pass
- [ ] Test coverage is decent (pages, state management, error handling, API endpoints should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: log in as admin → navigate to /admin/roles → load users → select users → change role → confirm → success toast and table updates. Expand a user → toggle permission → confirm → updates. Check audit log. Log in as regular user → navigate to /settings/permissions → see role and permissions → click 'Request Access' → submit → pending request appears. Try to access /admin/roles as non-admin → redirected with error. Simulate network failure → see error handling. All interactions should feel smooth with no errors.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Whether the API tests cover edge cases (race conditions, transaction rollback, session invalidation
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: