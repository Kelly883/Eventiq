# Step 17 of 345: Set up folder structure for check-in system

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_attendee-check-in-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an attendee check-in system for events. This is a venue staff tool that lets event organizers and staff check in attendees at the door using QR codes or manual search.

On the frontend (React + Vite), create a check-in feature area under src/features/check-in/ with subfolders: components (for reusable UI pieces like search bars, QR scanners, stats displays), hooks (for data fetching and state logic), pages (for the main check-in interface), types (for TypeScript interfaces), and utils (for helpers like QR decoding and offline sync logic).

On the backend (Laravel), create a check-in feature area under app/Http/Controllers/Venue/ with subfolders: CheckInController (for the six API endpoints), Requests (for validating incoming data), Resources (for formatting API responses), and Services (for business logic like fraud detection and offline sync handling).

Also create a database migrations folder under database/migrations/ — we'll need to set up tables for tracking check-ins, fraud events, and audit logs.

This feature has six endpoints and needs to handle real-time updates, offline sync, and fraud detection, so keeping the code organized from the start will make it much easier to maintain.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] Frontend check-in feature folder exists with components/, hooks/, pages/, types/, and utils/ subfolders
- [ ] Backend Venue folder exists under app/Http/Controllers/ with CheckInController, Requests, Resources, and Services subfolders
- [ ] database/migrations/ folder exists
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Run npm run dev on the frontend and php artisan serve on the backend — both should start without errors. This is just scaffolding, so nothing functional yet.

Then give me your honest advice on:
- Whether this folder structure makes sense for a Laravel + React project
- If there's a better way to organize the check-in logic given the existing project structure
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: