# Step 69 of 345: Create user_dashboard_preferences table and indexes

================================================================================

**Category:** Database Setup
**Flow:** feature_user-dashboard-ticket-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the database table that stores user dashboard preferences and create indexes for fast lookups.

Create a Laravel migration for the user_dashboard_preferences table with these fields: id (UUID primary key), userId (string, foreign key to users.id, indexed), defaultTicketFilter (string, default 'all'), defaultDateRange (string, default '30days'), showRecommendations (boolean, default true), showActivityFeed (boolean, default true), autoRefreshEnabled (boolean, default true), createdAt (timestamp), updatedAt (timestamp).

Add a unique index on userId to enforce one-to-one relationship. Add a regular index on userId for fast lookups when querying preferences. The migration should include a foreign key constraint on userId that cascades on delete (if a user is deleted, their preferences are too).

Run the migration to create the table. Then create a UserDashboardPreference Eloquent model at app/Models/UserDashboardPreference.php with proper relationships to the User model.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database is properly set up.

Confirm that:
- [ ] The migration runs without errors (php artisan migrate)
- [ ] The user_dashboard_preferences table exists with all fields
- [ ] The userId column is indexed and has a unique constraint
- [ ] Foreign key constraint is in place
- [ ] The UserDashboardPreference model exists and has a relationship to User
- [ ] You can create and query preferences without errors

Test the actual flow: Run the migration. Use Tinker to create a test preference record. Query it back and verify all fields are correct.

Then give me your honest advice on:
- Whether the schema matches the requirements
- Any missing indexes that could slow down queries
- Edge cases (what happens if a user is deleted?)
- Anything you'd change before going live
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: