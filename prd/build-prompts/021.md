# Step 21 of 345: Set up folder structure for revenue settlement

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_revenue-settlement-payouts

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a revenue settlement and payout system for organizers and admins. This is a complex financial feature with two separate dashboards, so we need a clean folder structure.

On the frontend (React + Vite), create a payouts feature area at src/features/payouts/ with these subfolders:
- pages/ (for OrganizerPayoutDashboardPage and AdminSettlementDashboardPage)
- components/ (reusable UI pieces like PayoutTable, SummaryCards, StatusBadges, ExpandablePayoutRow, FilterBar, ExportButton)
- hooks/ (custom React hooks for data fetching and state management)
- types/ (TypeScript interfaces matching the backend schemas)
- utils/ (helper functions for calculations, formatting, date ranges)
- services/ (API client functions)

On the backend (Laravel), create a Payouts feature with:
- Models/ (Payout, PayoutCalculation, SettlementPolicy models)
- Controllers/ (OrganizerPayoutController for organizer endpoints, AdminSettlementController for admin endpoints)
- Policies/ (PayoutPolicy for authorization)
- Migrations/ (database schema files)
- Requests/ (form request validation classes)
- Resources/ (API resource classes for JSON responses)

This feature touches sensitive financial data, so we'll be strict about authorization and logging.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The payouts feature folder exists on the frontend with pages/, components/, hooks/, types/, utils/, and services/ subfolders
- [ ] The backend has Models/, Controllers/, Policies/, Migrations/, Requests/, and Resources/ folders for payouts
- [ ] The project still builds without errors after adding these folders
- [ ] No TypeScript or PHP errors appear in the IDE

Test the actual flow: Just run a build on both frontend and backend — this is scaffolding only.

Then give me your honest advice on:
- Whether this folder structure makes sense for a financial feature with two separate dashboards
- If there's a better way to organize the backend controllers (separate by organizer vs. admin concerns)
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: