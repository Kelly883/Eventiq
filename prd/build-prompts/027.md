# Step 27 of 345: Set up payment feature folder structure

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_payment-gateway-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a payment gateway integration with Stripe. For this step, I need you to set up the folder structure for the payment feature.

On the frontend (React + Vite), create a payment feature area under src/features/payment/ with these subfolders:
- pages (for PaymentMethodsPage and OrganizerPaymentSettingsPage)
- components (for reusable UI pieces like PaymentMethodCard, StripeElementsForm, ConnectStatusDisplay)
- hooks (for custom React hooks that manage payment state and API calls)
- types (for TypeScript interfaces related to payments)
- utils (for Stripe configuration and helper functions)

On the backend (Laravel), create a payment feature area under app/Features/Payment/ with these subfolders:
- Controllers (PaymentMethodController, OrganizerPaymentController, WebhookController)
- Models (PaymentMethod, Organizer enhancements)
- Policies (PaymentMethodPolicy, OrganizerPolicy)
- Requests (form request validation classes)
- Jobs (for async processing of webhook events)
- Services (PaymentService, StripeConnectService for business logic)

Also create a database/migrations folder if it doesn't exist — we'll add migrations for the new payment-related tables.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The frontend payment feature folder exists with pages/, components/, hooks/, types/, and utils/ subfolders
- [ ] The backend payment feature folder exists with Controllers/, Models/, Policies/, Requests/, Jobs/, and Services/ subfolders
- [ ] The database/migrations folder exists
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Just run a build on both frontend and backend — this is scaffolding only.

Then give me your honest advice on:
- Whether this folder structure makes sense for the number of files we'll have
- If there's a better way to organize things given the project's existing structure
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: