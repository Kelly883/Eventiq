# Step 75 of 345: Create database migrations for payouts and settlements

================================================================================

**Category:** Database Setup
**Flow:** feature_revenue-settlement-payouts

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the database schema for the three main tables: payouts, payout_calculations, and settlement_policies.

Create Laravel migrations for each:

1. settlement_policies migration — This table stores the rules for different organizer tiers. Fields: id (UUID), organizerTier (string: standard/premium/enterprise), platformCommissionPercentage (decimal 5,2), processingFeePercentage (decimal 5,2), payoutFrequency (string: daily/weekly/biweekly/monthly/on_demand), minimumPayoutThreshold (decimal 10,2), payoutHoldDays (integer), requiresApproval (boolean), autoApproveThreshold (decimal 10,2 nullable), maxRetries (integer), retryBackoffMultiplier (decimal 3,2), taxWithholdingPercentage (decimal 5,2 nullable), allowedPayoutMethods (JSON array), isActive (boolean), timestamps. Add a unique index on organizerTier.

2. payouts migration — This is the main payout record. Fields: id (UUID), organizerId (UUID, foreign key to organizers), settlementPeriodStartDate (timestamp), settlementPeriodEndDate (timestamp), grossRevenue (decimal 12,2), refundsDeducted (decimal 12,2), netRevenue (decimal 12,2), platformCommissionPercentage (decimal 5,2), platformCommissionAmount (decimal 12,2), processingFeePercentage (decimal 5,2), processingFeeAmount (decimal 12,2), taxWithholdingPercentage (decimal 5,2 nullable), taxWithholdingAmount (decimal 12,2 nullable), payoutAmount (decimal 12,2), payoutMethod (string), paymentGatewayPayoutId (string nullable), paymentGatewayResponse (JSON nullable), status (string: pending/calculated/approved/processing/completed/failed), calculatedAt (timestamp nullable), approvedBy (UUID nullable, foreign key to users), approvedAt (timestamp nullable), processingStartedAt (timestamp nullable), completedAt (timestamp nullable), failureReason (text nullable), retryCount (integer default 0), nextRetryAt (timestamp nullable), timestamps. Add indexes on organizerId, status, settlementPeriodStartDate, settlementPeriodEndDate, and a composite index on (organizerId, status).

3. payout_calculations migration — Immutable audit trail. Fields: id (UUID), payoutId (UUID, foreign key to payouts), organizerId (UUID), settlementPeriodStartDate (timestamp), settlementPeriodEndDate (timestamp), eventIds (JSON array), orderIds (JSON array), refundRequestIds (JSON array), totalOrderCount (integer), totalTicketsSold (integer), totalRefundsProcessed (integer), calculationDetails (JSON), calculatedAt (timestamp), calculatedBy (string), createdAt (timestamp). Add indexes on payoutId and organizerId. This table is append-only (never updated).

Run the migrations: php artisan migrate

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database schema is set up correctly.

Confirm that:
- [ ] All three migrations run without errors (php artisan migrate)
- [ ] The settlement_policies table exists with all fields and the organizerTier index
- [ ] The payouts table exists with all fields and proper indexes (organizerId, status, date ranges, composite)
- [ ] The payout_calculations table exists as append-only with payoutId and organizerId indexes
- [ ] Foreign keys are properly set up (organizerId → organizers, approvedBy → users, payoutId → payouts)
- [ ] All decimal fields use the correct precision (12,2 for amounts, 5,2 for percentages)
- [ ] JSON fields are properly typed (eventIds, orderIds, refundRequestIds, allowedPayoutMethods, calculationDetails, paymentGatewayResponse)

Test the actual flow: Run php artisan migrate:fresh to reset and reapply all migrations. Then use Laravel Tinker to verify the tables exist: php artisan tinker → Schema::getColumns('payouts') → should show all fields. Insert a test settlement policy and payout record to verify the schema works.

Then give me your honest advice on:
- Whether the indexes cover all the queries we'll need (filtering by organizerId, status, date ranges)
- Any missing fields that a real payout system would need
- Performance concerns with large payout volumes (millions of records)
- Whether the JSON fields are the right choice or if we should normalize further
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: