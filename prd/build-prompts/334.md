# Step 334 of 345: Test the pricing feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_dynamic-pricing-sales-windows

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the pricing feature doesn't break and handles all the important paths.

Unit tests:
- usePricingWindows hook: fetching windows populates the list, adding a window optimistically updates then syncs, updating a window works the same way, deleting a window works the same way, failed operations revert the local state
- usePricingForm hook: validation checks all required fields, detects overlapping windows, detects negative prices, detects invalid dates, reset clears all fields
- useTimelineData hook: sorts windows by start date, computes isActive correctly, computes daysUntilStart and daysUntilEnd, computes percentageComplete
- Error message mapper: each error code maps to the right friendly message, unknown codes use fallback

Component tests:
- EventPricingConfigPage: loading state shows skeleton cards, empty state shows 'No pricing rules yet', displaying windows shows all fields, adding a window opens the form, saving persists changes, preview button opens the modal
- PricingPreviewModal: loading state shows skeleton timeline, success state shows all categories and windows, error state shows 'Unable to load preview', close button closes the modal
- PricingWindowForm: all fields render, validation errors show below fields, submit button is disabled until form is valid, submitting calls the right API endpoint
- TimelineVisualization: windows are color-coded correctly (early bird=green, regular=blue, last-minute=orange), windows are sorted by start date, active windows are highlighted

Integration tests (full user flows):
- Create a new pricing window: open config page → click 'Add Pricing Window' → fill in form → submit → window appears in list → refresh page → window is still there
- Update a pricing window: open config page → click edit on a window → change the price → submit → window updates in list → refresh page → change persists
- Delete a pricing window: open config page → click delete on a window → confirm → window disappears from list → refresh page → window is gone
- Preview pricing: open config page → click 'Preview' → modal opens → shows all windows → close modal → back on config page
- Error handling: try adding a window with overlapping dates → validation error → fix it → submit → succeeds. Try adding with negative price → validation error → fix it → submit → succeeds. Lose internet while adding → error notification → regain internet → retry → succeeds.

API endpoint tests:
- GET /api/organizer/events/:eventId/pricing/windows: returns 200 with windows for the event, returns 401 without auth, returns 403 if user doesn't own event, returns 404 if event doesn't exist
- POST /api/organizer/events/:eventId/pricing/windows: creates a window and returns 201, returns 400 for invalid input, returns 401 without auth, returns 403 if user doesn't own event, returns 409 for overlapping windows
- PATCH /api/organizer/events/:eventId/pricing/windows/:windowId: updates a window and returns 200, returns 400 for invalid input, returns 401 without auth, returns 403 if user doesn't own event, returns 404 if window doesn't exist
- DELETE /api/organizer/events/:eventId/pricing/windows/:windowId: soft-deletes a window and returns 200, returns 401 without auth, returns 403 if user doesn't own event, returns 404 if window doesn't exist
- GET /api/organizer/events/:eventId/pricing/preview: returns 200 with preview data, returns 401 without auth, returns 403 if user doesn't own event, returns 404 if event doesn't exist
- GET /api/attendee/events/:eventId/pricing: returns 200 with public pricing data for published events, returns 404 for unpublished events, returns 404 if event doesn't exist

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (hooks, error mapper)
- [ ] All component tests pass (pages, modals, forms, timeline)
- [ ] All integration tests pass on a real device
- [ ] All API endpoint tests pass
- [ ] Test coverage is decent (hooks, components, and pages should be well-covered)
- [ ] No flaky tests (tests that sometimes pass, sometimes fail)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: open config page → ad
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: