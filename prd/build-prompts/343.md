# Step 343 of 345: Test the compliance feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_audit-logging-compliance

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the compliance feature doesn't break.

Unit tests:
- Data masking utility: IP '192.168.1.1' becomes '192.168.***.*', sensitive fields are removed from request/response data
- Error message mapper: each error code maps to the right friendly message
- Audit log state: loading logs sets loading state, changing filters resets and reloads, polling appends new events
- Compliance report state: generating a report shows progress, polling completes, scheduling works

Component tests:
- AuditLogsTable: renders rows with timestamp, action, actor, target, status, metadata. Clicking a row expands details. Checkboxes work for bulk selection.
- AuditFilterBar: filters by date range, action type, actor role, target type, status, classification. Search box works and is debounced.
- AuditExportButton: clicking opens a modal with format options (CSV/JSON). Exporting shows a confirmation.
- ReportTemplateCard: shows report type, description, and 'Generate' button. Clicking opens the generation modal.
- ReportGenerationModal: date range selector, classification filter, format options. Submitting generates the report.
- ComplianceChecklist: displays compliance items with green/amber/red status indicators.

Integration tests (full user flows):
- Admin logs in → navigates to AuditLogsViewerPage → sees audit logs in table → filters by action type → exports as CSV → sees confirmation and download link
- Admin navigates to ComplianceReportsPage → clicks 'Generate' on Payment Audit Trail → sets date range → selects PDF format → sees progress indicator → report completes and appears in preview pane
- Admin bulk-selects multiple audit logs → tags them for 'compliance_review' → sees confirmation and list refreshes
- Admin schedules a report for weekly generation → sees confirmation → checks the scheduled reports list
- Non-admin tries to access /admin/compliance/audit-logs → redirected to not authorized page
- Admin's session expires while viewing compliance page → redirected to login → signs in → back on compliance page

Also test the API endpoints: make sure all five return the right responses with valid admin auth, reject without auth, reject non-admins, and handle bad inputs (invalid date ranges, missing required fields, etc.).

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (data masking, error messages, state management)
- [ ] All component tests pass (tables, filters, modals, checklists)
- [ ] All integration tests pass on a real device or test environment
- [ ] API endpoint tests pass (valid admin auth, no auth, non-admin, bad inputs)
- [ ] Test coverage is decent (state management, components, and integration flows should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration tests: log in as admin → navigate to audit logs → filter by action → export as CSV → see confirmation. Navigate to reports → generate a payment audit report → see progress → report completes. Bulk-select logs → tag them → see confirmation. Schedule a report → see confirmation. Log in as non-admin → try accessing audit logs → redirected. Let session expire → redirected to login → sign in → back on compliance page.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Whether the mock API responses are realistic
- Tests you'd add before feeling confident about a release
- Performance test scenarios (large audit logs, slow network, etc.)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: