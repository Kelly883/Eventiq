# Step 122 of 345: Set up routes for delivery pages

================================================================================

**Category:** Navigation & Routing
**Flow:** feature_ticket-delivery-distribution

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to add three new routes to the React router for the delivery feature pages.

Add these routes to your router configuration (likely in src/router.tsx or src/App.tsx):

1. /my-tickets/:ticketId/status — Renders TicketStatusPage. This is a protected route (user must be authenticated). The :ticketId parameter is extracted from the URL and passed to the page component.

2. /settings/delivery-preferences — Renders DeliverySettingsPage. Protected route for authenticated users. This is a settings page, so it might be nested under a settings layout or section.

3. /admin/delivery/dashboard — Renders AdminDeliveryDashboardPage. This is an admin-only route — add a check to verify the user has the 'Admin' role before rendering. If not an admin, redirect to /dashboard or show an access denied message.

For the admin route, implement a ProtectedAdminRoute wrapper component that checks the user's role before rendering. If the user is not an admin, redirect to the home page or show a 403 error page.

Make sure all three routes are added to your router's route configuration and that the page components are properly imported. Use React Router's lazy loading if the pages are large to improve initial load time.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all three routes are properly configured and accessible.

Confirm that:
- [ ] /my-tickets/:ticketId/status route exists and renders TicketStatusPage
- [ ] /settings/delivery-preferences route exists and renders DeliverySettingsPage
- [ ] /admin/delivery/dashboard route exists and renders AdminDeliveryDashboardPage
- [ ] All three routes require authentication (redirect to login if not authenticated)
- [ ] The admin route checks for admin role (redirect if not admin)
- [ ] URL parameters are correctly passed to page components (:ticketId)
- [ ] The routes are accessible via direct URL navigation

Test the actual flow: Navigate to /my-tickets/test-id/status while logged in — TicketStatusPage should render. Navigate to /settings/delivery-preferences — DeliverySettingsPage should render. Navigate to /admin/delivery/dashboard as a non-admin user — should redirect or show access denied. Navigate as an admin — should render AdminDeliveryDashboardPage.

Then give me your honest advice on:
- Whether the route structure makes sense for the app's navigation hierarchy
- If there are any missing routes or nested route structures
- How the admin route protection integrates with the rest of the app's auth system
- Whether lazy loading should be used for these pages
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: