# Step 77 of 345: Create auditLogs table and indexes for compliance

================================================================================

**Category:** Database Setup
**Flow:** feature_audit-logging-compliance

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the database schema for the immutable auditLogs collection. This table will store every system transaction, user action, and data modification for regulatory compliance.

Create a Laravel migration that creates the auditLogs table with these fields:
- id (UUID primary key, auto-generated)
- user_id (string, foreign key to users table, indexed)
- action (string, enum or varchar: user_login, user_logout, user_suspended, event_created, event_approved, event_flagged, event_cancelled, payment_processed, payment_refunded, refund_requested, refund_approved, refund_rejected, payout_approved, payout_rejected, ticket_checked_in, ticket_voided, fraud_flagged, fraud_approved, admin_setting_changed, user_permission_changed, data_export_requested)
- target_type (string, enum or varchar: user, event, order, payment, refund, payout, ticket, fraud_event, admin_setting, permission)
- target_id (string, indexed for fast lookups)
- status (string, enum: success, failed, pending)
- ip_address (string, hashed for privacy, indexed)
- user_agent (text)
- geolocation (JSON: {country, city, latitude, longitude})
- request_data (JSON, sanitized)
- response_data (JSON, sanitized)
- changed_fields (JSON, for updates: {fieldName: {before, after}})
- error_message (text, nullable)
- error_code (string, nullable)
- compliance_classification (string, enum: public, internal, sensitive, pci_dss, gdpr_personal_data, sox_financial)
- retention_date (timestamp, indexed for cleanup queries)
- metadata (JSON: {sessionId, requestId, correlationId, duration_ms, dataSize_bytes, tags: []})
- created_at (timestamp, indexed, immutable)
- updated_at (timestamp)

Create indexes on: (user_id), (target_id), (ip_address), (created_at), (retention_date), (user_id, created_at), (action, status), (compliance_classification). These indexes ensure fast filtering and sorting for the audit log viewer and report generation.

Make the table immutable by setting created_at and user_id as non-updatable in the model. Implement soft deletes so logs are never truly deleted (mark deleted, retain data for compliance).

Run the migration: `php artisan migrate`

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database is properly configured for audit logging.

Confirm that:
- [ ] The auditLogs table exists with all required fields
- [ ] All indexes are created (user_id, target_id, ip_address, created_at, retention_date, composite indexes)
- [ ] The table supports soft deletes
- [ ] created_at and user_id are immutable in the model
- [ ] The migration runs without errors

Test the actual flow: Run `php artisan migrate` and verify the table structure with `php artisan tinker` or a database client. Insert a test audit log record and verify all fields are stored correctly.

Then give me your honest advice on:
- Whether the indexes cover all the queries we'll need for filtering and sorting
- Performance concerns with storing large JSON objects (request_data, response_data) at scale
- Whether the soft delete approach is appropriate for compliance (some regulations require permanent deletion after retention period)
- Edge cases (what happens when retention_date is reached, how to handle cleanup)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: