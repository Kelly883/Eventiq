# Step 96 of 345: Create TypeScript types and Laravel models for dashboard data

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_user-dashboard-ticket-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to define the data structures that flow between the frontend and backend.

On the frontend, create TypeScript types at src/features/dashboard/types/index.ts and src/features/tickets/types/index.ts. Define: Ticket (id, eventId, eventTitle, eventDate, eventVenue, tierId, tierName, qrCodeData, status, deliveryStatus, deliveryMethod, deliveryTimestamp, orderId), Event (id, title, date, venue, organizer), TicketFilter ('all' | 'upcoming' | 'past'), DateRange ('7days' | '30days' | '90days' | 'all'), DashboardSummary (totalTickets, upcomingEventsCount, pastEventsCount, totalSpent), ActivityItem (eventTitle, tierName, purchaseDate, deliveryStatus), DashboardPreferences (defaultTicketFilter, defaultDateRange, showRecommendations, showActivityFeed, autoRefreshEnabled), DeliveryEvent (timestamp, status, method, errorMessage).

On the Laravel backend, create models: UserDashboardPreference (already created in database_setup), and ensure Ticket, Event, Order, and DeliveryEvent models exist with proper relationships. Add accessors to the Ticket model for computed fields like deliveryStatus (derived from delivery_events). Create API Resources at app/Http/Resources/ for: TicketResource, DashboardOverviewResource, PreferencesResource that transform the models into the JSON structure the frontend expects.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the types and models are correctly defined.

Confirm that:
- [ ] All TypeScript types are exported and can be imported without errors
- [ ] Ticket type includes all required fields
- [ ] DashboardPreferences type matches the database schema
- [ ] UserDashboardPreference model has proper relationships to User
- [ ] API Resources transform models correctly
- [ ] A Ticket model instance can be converted to TicketResource without errors

Test the actual flow: Import the types in a test component. Create a mock Ticket object and verify it matches the type. On the backend, query a ticket and convert it to TicketResource — verify the JSON output is correct.

Then give me your honest advice on:
- Whether the types cover all the data we need
- Any missing fields that would be useful
- Whether the API Resources are transforming data correctly
- Edge cases (null values, missing relationships)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: