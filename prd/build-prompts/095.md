# Step 95 of 345: Create DeliveryEvent and DeliveryPreference models

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_ticket-delivery-distribution

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need two Eloquent models to represent the delivery data in our application.

Create the DeliveryEvent model at app/Models/DeliveryEvent.php. This model represents a single ticket delivery attempt. Add properties for all 25 fields from the delivery_events table. Define relationships: belongsTo Ticket, belongsTo Order, belongsTo User, belongsTo Event, belongsTo FraudEvent (nullable). Add a scope method scopeForUser($query, $userId) to filter delivery events by user. Add a scope scopeByStatus($query, $status) to filter by delivery status. Add a helper method getStatusBadgeColor() that returns 'green' for sent, 'amber' for pending, 'red' for failed. Add a method canRetry() that checks if attemptCount < maxAttempts and the delivery hasn't been blocked. Cast the providerResponse field to array and timestamps to Carbon instances.

Create the DeliveryPreference model at app/Models/DeliveryPreference.php. This represents user delivery settings. Add properties for all 17 fields. Define a relationship: belongsTo User. Add a method getEnabledMethods() that returns an array of enabled delivery methods (email, sms, dashboard). Add a method getPrimaryMethodStatus() that returns 'verified' or 'unverified' based on the primary method's verification status. Add a method getBackupMethodStatus() similarly. Cast boolean fields appropriately.

Both models should use soft deletes (add SoftDeletes trait) to retain data for audit purposes. Make both models fillable for mass assignment where appropriate, but guard sensitive fields like emailVerified and phoneVerified from direct assignment.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both models work correctly and have the right relationships.

Confirm that:
- [ ] DeliveryEvent model has all 25 fields as properties
- [ ] DeliveryEvent relationships work: can access $deliveryEvent->ticket, $deliveryEvent->user, etc.
- [ ] DeliveryEvent scopes work: DeliveryEvent::forUser($userId)->get() returns only that user's events
- [ ] DeliveryEvent helper methods work: getStatusBadgeColor() returns correct colors, canRetry() checks limits
- [ ] DeliveryPreference model has all 17 fields as properties
- [ ] DeliveryPreference relationships work: can access $prefs->user
- [ ] DeliveryPreference helper methods work: getEnabledMethods() returns array, status methods return 'verified' or 'unverified'
- [ ] Both models use soft deletes (trashed records are excluded by default)

Test the actual flow: Create a test DeliveryEvent and DeliveryPreference in the database. Load them via the models and verify all fields are accessible. Test the scopes and helper methods.

Then give me your honest advice on:
- Whether the relationships are set up correctly (no missing foreign keys)
- If there are any fields that should be hidden or encrypted (like email/phone hashes)
- Whether the helper methods cover all the use cases (status badges, retry logic)
- Performance concerns with eager loading relationships on large result sets
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: