# Step 18 of 345: Set up folder structure for email notifications

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_email-notification-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an email notification system with an admin interface for managing templates. For this step, I need you to set up the folder structure for both the frontend and backend.

On the frontend (React + Vite), create a feature folder at src/features/email-notifications/ with subfolders for: pages (AdminEmailTemplateManagementPage), components (TemplateList, TemplateEditor, VariablesSidebar, TestEmailModal), hooks (useEmailTemplates, useTemplateForm), types (EmailTemplate interface), and utils (template validation, placeholder parsing).

On the backend (Laravel), create a feature folder at app/Features/EmailNotifications/ with subfolders for: Controllers (EmailTemplateController), Models (EmailTemplate), Requests (StoreEmailTemplateRequest, UpdateEmailTemplateRequest), Policies (EmailTemplatePolicy), Mails (TestEmailMailable), and Services (EmailTemplateService for business logic).

Also create a database/migrations folder for the email_templates table migration, and a resources/views/emails folder for email template previews.

This structure keeps email logic isolated and makes it easy to extend later with more notification types.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The frontend feature folder exists with pages/, components/, hooks/, types/, and utils/ subfolders
- [ ] The backend feature folder exists with Controllers/, Models/, Requests/, Policies/, Mails/, and Services/ subfolders
- [ ] The database/migrations and resources/views/emails folders exist
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Just run a build on both frontend and backend to make sure nothing is broken — this is scaffolding only.

Then give me your honest advice on:
- Whether this folder structure makes sense for the number of files we'll have
- If there's a better way to organize things given the project's existing structure
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: