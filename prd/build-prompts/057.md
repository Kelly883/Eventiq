# Step 57 of 345: Create organizers table and audit logs table

================================================================================

**Category:** Database Setup
**Flow:** feature_organizer-profile-branding

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the database tables for the organizer profile system.

1. Organizers table — Create a migration that creates the organizers table with these columns: id (UUID primary key), userId (UUID, foreign key to users.id, indexed), displayName (string, required), bio (text, nullable, max 500 chars enforced in app), avatarUrl (string, nullable), email (string, nullable), phone (string, nullable), website (string, nullable), socialLinks (JSON, nullable, stores {twitter, instagram, linkedin, youtube}), brandingColors (JSON, nullable, stores {primaryColor, accentColor}), isPublic (boolean, default true), emailPublic (boolean, default false), phonePublic (boolean, default false), notificationPreferences (JSON, nullable, stores {ticketSales, eventReminders, platformUpdates}), totalEventsCreated (integer, default 0), totalTicketsSold (integer, default 0), createdAt (timestamp), updatedAt (timestamp), deletedAt (timestamp, nullable for soft deletes).

Create indexes on: userId (for fast lookups), userId + isPublic (for filtering public profiles), createdAt (for sorting).

2. Audit logs table — Create a migration for auditLogs with columns: id (UUID primary key), adminId (UUID, foreign key to users.id), targetUserId (UUID, foreign key to users.id), action (string, e.g., 'profile_updated', 'avatar_uploaded'), oldValue (JSON, nullable), newValue (JSON, nullable), createdAt (timestamp). Index on targetUserId + createdAt for fast lookups.

Run php artisan migrate to apply both migrations.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database tables are properly set up.

Confirm that:
- [ ] The organizers table exists with all required columns
- [ ] The auditLogs table exists with all required columns
- [ ] Indexes are created on userId, userId+isPublic, and createdAt for organizers
- [ ] Indexes are created on targetUserId+createdAt for auditLogs
- [ ] The migrations ran without errors
- [ ] Foreign keys are properly set up (userId references users.id)
- [ ] Soft deletes are enabled on the organizers table (deletedAt column exists)

Test the actual flow: Run php artisan migrate. Then use a database client to verify the table structure. Insert a test organizer record and verify all columns accept the right data types.

Then give me your honest advice on:
- Whether the JSON columns will perform well with large socialLinks or brandingColors objects
- If the denormalized counts (totalEventsCreated, totalTicketsSold) need triggers to stay in sync
- Edge cases (what happens if a user is deleted but their organizer profile remains?)
- Anything you'd add to the schema before going live
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: