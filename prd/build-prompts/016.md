# Step 16 of 345: Set up folder structure for QR code ticketing

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_qr-code-ticket-generation-verification

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a QR code ticket generation and venue check-in system. For this step, I need you to set up the folder structure for both the React frontend and Laravel backend.

On the frontend side (React + Vite), create a features folder at src/features/qr-code-ticketing/ with subfolders for: pages (we'll have VenueCheckInPage), components (QRScanner, TicketCard, SearchBar, OfflineIndicator, StatusBadge), hooks (useQRScanner, useOfflineSync, useCheckInForm), services (qrVerificationService, offlineCacheService), types (for TypeScript interfaces), and utils (encryption helpers, validation functions).

On the backend side (Laravel), create a new feature module at app/Features/QRCodeTicketing/ with subfolders for: Controllers (QRGenerationController, QRVerificationController, VenueCheckInController, CheckInAnalyticsController), Requests (form request validation), Services (QRCodeService, EncryptionService, OfflineSyncService), Models (extend the Ticket model), and Policies (QRCodePolicy for authorization).

Also create a database migrations folder for the new ticket fields we'll add. This separation keeps QR code logic isolated from the rest of the ticketing system.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The React frontend has src/features/qr-code-ticketing/ with pages/, components/, hooks/, services/, types/, and utils/ subfolders
- [ ] The Laravel backend has app/Features/QRCodeTicketing/ with Controllers/, Requests/, Services/, Models/, and Policies/ subfolders
- [ ] Both the frontend and backend still build without errors after adding these folders
- [ ] The project structure follows the existing conventions for both frameworks

Test the actual flow: Just run a build on both frontend and backend to make sure nothing is broken — this is scaffolding only.

Then give me your honest advice on:
- Whether this folder structure makes sense for the number of files we'll have
- If there's a better way to organize things given the project's existing structure
- Anything you'd change before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: