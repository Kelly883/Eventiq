# Step 126 of 345: Wire email templates into admin navigation

================================================================================

**Category:** Navigation & Routing
**Flow:** feature_email-notification-system

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to add the email template management page to the admin section of the app.

Add a route for /admin/settings/email-templates that renders the AdminEmailTemplateManagementPage. This page should only be accessible to users with the 'Admin' role — enforce this with a route guard that checks the user's role before rendering the page. If a non-admin tries to access it, redirect to a 403 'Access Denied' page.

Add a navigation link in the admin settings menu (or sidebar) that says 'Email Templates' and points to /admin/settings/email-templates. This link should only appear for admin users.

Wire up the page to use react-router for navigation and swr for data fetching. The page should fetch the list of templates on mount using the GET /api/admin/email-templates endpoint.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the email template page is properly wired into the admin section.

Confirm that:
- [ ] The route /admin/settings/email-templates exists and renders AdminEmailTemplateManagementPage
- [ ] Non-admin users cannot access the page (redirected to 403)
- [ ] Admin users can access the page
- [ ] The 'Email Templates' link appears in the admin settings menu for admin users
- [ ] Clicking the link navigates to /admin/settings/email-templates
- [ ] The page fetches templates on mount
- [ ] The page loads and displays the template list

Test the actual flow: Log in as a non-admin user → try to navigate to /admin/settings/email-templates → see 403 'Access Denied'. Log in as an admin → see the 'Email Templates' link in the settings menu → click it → page loads and shows the template list.

Then give me your honest advice on:
- Whether the navigation feels natural and follows the app's existing patterns
- Any broken navigation paths or dead ends
- How the route guard handles edge cases (user role changes mid-session, expired auth)
- What would confuse a first-time admin user
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: