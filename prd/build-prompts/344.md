# Step 344 of 345: Test offline sync feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_mobile-app-responsive-design

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the offline sync feature works correctly and doesn't break.

Unit tests:
- OfflineSyncDevicesState: loading devices, toggling offline_enabled (optimistic + revert on error), updating last_sync_at
- CachedTicketsState: loading tickets, storing to localStorage, loading from localStorage, checking staleness, clearing data
- OfflineSyncStatus: starting sync, handling success, handling errors

Component tests:
- OfflineSyncSettingsPage: renders device list, toggle works, 'Sync Now' button works, error messages display
- OfflineTicketsPage: renders cached tickets grouped by event, displays QR codes, shows last sync timestamp, 'Refresh' button only appears when online

Integration tests (full user flows):
- User opens app → cached tickets load from localStorage if fresh → OfflineTicketsPage shows cached data
- User taps 'Sync Now' → loading state → sync completes → last_sync_at updates → confirmation message
- User toggles offline_enabled for a device → optimistic update → request succeeds → UI reflects change
- User goes offline → offline indicator appears → OfflineTicketsPage accessible → user goes online → offline indicator disappears
- User logs out → all cached data cleared from localStorage → log back in → no old data visible

Responsive design tests:
- OfflineSyncSettingsPage renders correctly on mobile (375px), tablet (768px), desktop (1024px)
- OfflineTicketsPage renders correctly on mobile, tablet, desktop
- No horizontal scrolling on any screen size
- Touch targets are at least 44x44 pixels

Also test the API endpoints: verify PATCH /api/notifications/device-tokens/:token/offline-status and GET /api/me/tickets/for-offline-sync return correct data with valid auth, reject without auth, and handle edge cases (invalid token, rate limit, server error).

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (state managers)
- [ ] All component tests pass (both pages)
- [ ] All integration tests pass on a real device
- [ ] API endpoint tests pass (valid auth, no auth, rate limit, server error)
- [ ] Responsive design tests pass on mobile, tablet, desktop
- [ ] Test coverage is decent (state managers and pages should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: open app → cached tickets load → tap 'Sync Now' → loading → sync completes → confirmation appears → toggle a device → update succeeds → go offline → offline indicator appears → OfflineTicketsPage accessible → go online → indicator disappears → log out → cached data cleared. Resize browser from mobile to tablet to desktop → verify layout adapts smoothly.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Responsive design edge cases not covered by automated tests
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: