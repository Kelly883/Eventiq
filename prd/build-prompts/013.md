# Step 13 of 345: Set up fraud detection feature folder structure

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_fraud-detection-prevention

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a fraud detection and prevention system for an e-commerce platform. This step sets up the folder structure for both the Laravel backend and React frontend.

On the backend (Laravel), create a fraud feature area under app/Features/Fraud/ with these subdirectories:
- Services/ (for fraud detection logic, SDK initialization, and risk calculation)
- Http/Controllers/ (for API endpoints)
- Http/Requests/ (for request validation)
- Models/ (for Eloquent models)
- Policies/ (for authorization)
- Jobs/ (for queued tasks like email notifications)
- Events/ (for fraud-related events)

On the frontend (React + Vite), create a fraud feature area under src/features/fraud/ with:
- pages/ (FraudDetectionDashboardPage, FraudTransactionReviewModal)
- components/ (reusable UI components for the dashboard)
- hooks/ (custom React hooks for data fetching and state management)
- services/ (API client functions)
- types/ (TypeScript interfaces)
- utils/ (helper functions for formatting, calculations)

This structure keeps fraud detection isolated and maintainable as the feature grows.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The Laravel app/Features/Fraud/ directory exists with all subdirectories (Services, Http/Controllers, Http/Requests, Models, Policies, Jobs, Events)
- [ ] The React src/features/fraud/ directory exists with all subdirectories (pages, components, hooks, services, types, utils)
- [ ] Both the Laravel and React projects still build without errors
- [ ] No files are missing or misplaced

Test the actual flow: Run `php artisan` on the backend and `npm run dev` on the frontend — both should start without errors.

Then give me your honest advice on:
- Whether this folder structure makes sense for the fraud detection feature scope
- If there are any additional directories needed for logging, configuration, or migrations
- Whether the separation between backend services and frontend pages is clear and maintainable
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: