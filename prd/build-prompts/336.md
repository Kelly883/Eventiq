# Step 336 of 345: Test the calendar feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_event-calendar-scheduling

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the calendar feature doesn't break.

Unit tests:
- Calendar state: selecting a date updates the selected date, changing view updates the view, updating filters triggers a re-query
- Filter state: updating a filter persists to localStorage, clearing filters resets all to defaults
- Inventory polling: starts when modal opens, stops when modal closes, handles poll failures gracefully
- Error handler: maps API error codes to user-friendly messages

Component tests:
- EventCalendarPage: loading state shows skeleton grid, error state shows retry button, calendar displays events with color-coded indicators
- CalendarDayDetailModal: loading state shows skeleton cards, empty state shows 'No events', event cards show all required fields (title, organizer, time, price, availability)
- FilterSidebar: category dropdown filters events, price range slider filters events, location search filters events
- SearchBar: debounces input, returns matching results, shows 'No results' when empty

Integration tests (full user flows):
- Browse calendar → select a date → modal opens with events for that day → click an event → navigate to event details → back button → modal still open → close modal → back to calendar
- Browse calendar → filter by category → events update → change price range → events update → clear filters → all events show again
- Browse calendar → search for an event → results appear → click a result → navigate to event details
- Browse calendar → wait 30 seconds → ticket availability updates without page refresh

API endpoint tests:
- GET /api/events/public/calendar: returns events grouped by date with color-coded availability
- GET /api/events/public/calendar/day/:date: returns all events for a specific date, respects filters
- GET /api/events/public/calendar/range: returns events for a date range, respects filters
- All endpoints: reject invalid query params with 400, enforce rate limit (30/min), only return published events

Performance tests:
- Calendar with 100+ events loads in under 2 seconds
- Polling every 30 seconds doesn't cause memory leaks
- Filtering and sorting are responsive (< 500ms)

Accessibility tests:
- Calendar is keyboard navigable (arrow keys to change dates, Enter to open modal)
- Color-coded indicators have text labels (not just color)
- Modal is properly announced to screen readers

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (state management, error handling)
- [ ] All component tests pass (pages, modals, filters, search)
- [ ] All integration tests pass on a real device
- [ ] All API endpoint tests pass (valid requests, invalid params, rate limiting, data filtering)
- [ ] Performance tests show calendar loads in < 2 seconds
- [ ] Polling doesn't cause memory leaks (check memory usage over 5 minutes)
- [ ] Accessibility tests pass (keyboard navigation, screen reader support)
- [ ] Test coverage is decent (state management and pages should be well-covered, > 80%)

Test the actual flow: Run the full test suite. Then run the integration tests on a real device: open calendar → filter by category → select a date → modal opens → click an event → event detail page → back → modal still open → close modal → back to calendar. Wait 30 seconds → ticket counts update. All interactions should feel smooth with no errors.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests cover the most important user paths
- Any flaky test risks (timing, network dependencies, state leaks between tests)
- Performance concerns (polling, large event lists, slow networks)
- Accessibility gaps (keyboard navigation, screen reader support)
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: