# Step 97 of 345: Create Ticket model with QR code fields

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_qr-code-ticket-generation-verification

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to extend the existing Ticket model in Laravel to include the new QR code fields. Update app/Models/Ticket.php to add the new attributes to the $fillable array: qrCodeData, qrCodeSecret, qrCodeGeneratedAt, qrCodeExpiresAt, checkedInAt, checkedInBy, qrCodeScannedCount, lastQrScanAt.

Add a relationship: belongsTo(User::class, 'checkedInBy') to get the staff member who checked in the ticket.

Add helper methods:
- isQrExpired() — returns true if current time > qrCodeExpiresAt
- isCheckedIn() — returns true if checkedInAt is not null
- isVoid() — returns true if status === 'void'
- getQrStatus() — returns 'valid', 'checked_in', 'void', 'expired', or 'fraud_flagged' based on current state
- incrementScanCount() — increments qrCodeScannedCount and updates lastQrScanAt

Also create a separate QRCodePayload class (or use a DTO) that represents the decrypted QR payload structure: {ticketId, eventId, userId, email, tier, expiresAt}. This keeps the encryption/decryption logic separate from the model.

Make sure the Ticket model is immutable for createdAt (as specified in the schema).

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the Ticket model is properly extended with QR code functionality.

Confirm that:
- [ ] All eight new QR code fields are in the $fillable array
- [ ] The belongsTo relationship to User (checkedInBy) works correctly
- [ ] isQrExpired() returns true for expired QRs and false for valid ones
- [ ] isCheckedIn() returns true only if checkedInAt is set
- [ ] isVoid() returns true only if status is 'void'
- [ ] getQrStatus() returns the correct status string based on ticket state
- [ ] incrementScanCount() increments the count and updates the timestamp
- [ ] QRCodePayload DTO can be created from decrypted JSON
- [ ] createdAt is immutable (can't be changed after creation)

Test the actual flow: Create a test ticket, set some QR fields, and verify all the helper methods work. Create a QRCodePayload from test data. Check that the relationship loads the staff member correctly.

Then give me your honest advice on:
- Whether the model structure is clean and maintainable
- Any missing helper methods that would be useful
- Performance concerns (N+1 queries when loading checkedInBy)
- Edge cases (what if a ticket is checked in multiple times?)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: