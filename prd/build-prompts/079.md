# Step 79 of 345: Create database tables and migrations for preferences

================================================================================

**Category:** Database Setup
**Flow:** feature_accessibility-localization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up two database tables to store user preferences: accessibility_preferences and localization_preferences.

For accessibility_preferences:
- Create a Laravel migration that creates the table with columns: id (UUID primary key), userId (string, foreign key to users.id, indexed), fontSize (integer, default 16), highContrast (boolean, default false), screenReaderOptimized (boolean, default false), focusIndicatorEnhanced (boolean, default false), motionReduced (boolean, default false), lineHeight (decimal 3,2, default 1.5), letterSpacing (decimal 4,3, default 0), wordSpacing (decimal 4,3, default 0), colorBlindnessMode (string, default 'none'), createdAt and updatedAt timestamps.
- Add a unique index on userId (one-to-one relationship)
- Add a soft delete column (deleted_at) for soft deletes

For localization_preferences:
- Create a Laravel migration that creates the table with columns: id (UUID primary key), userId (string, foreign key to users.id, indexed), language (string, default 'en'), region (string, nullable), dateFormat (string, default 'MM/DD/YYYY'), timeFormat (string, default '12-hour'), currency (string, nullable), numberFormat (string, default 'comma'), rtlEnabled (boolean, default false), createdAt and updatedAt timestamps.
- Add a unique index on userId (one-to-one relationship)
- Add a soft delete column (deleted_at) for soft deletes

Run the migrations: php artisan migrate. Verify both tables exist in the database with all columns and indexes.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database tables are properly created.

Confirm that:
- [ ] Both migrations run without errors (php artisan migrate)
- [ ] accessibility_preferences table exists with all columns: id, userId, fontSize, highContrast, screenReaderOptimized, focusIndicatorEnhanced, motionReduced, lineHeight, letterSpacing, wordSpacing, colorBlindnessMode, createdAt, updatedAt, deleted_at
- [ ] localization_preferences table exists with all columns: id, userId, language, region, dateFormat, timeFormat, currency, numberFormat, rtlEnabled, createdAt, updatedAt, deleted_at
- [ ] Both tables have unique indexes on userId
- [ ] Both tables have soft delete support (deleted_at column)
- [ ] Foreign key constraints are set up correctly

Test the actual flow: Run php artisan tinker and manually insert a test record into each table. Verify the data is stored correctly and timestamps are set. Then query it back and confirm all fields are present.

Then give me your honest advice on:
- Whether the column types and defaults match the schema specifications
- Any missing indexes that would slow down queries
- Edge cases (what happens if a user is deleted but their preferences remain?)
- Whether soft deletes are the right approach or if we should hard delete
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: