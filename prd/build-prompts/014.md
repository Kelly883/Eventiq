# Step 14 of 345: Set up folder structure for ticket delivery

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_ticket-delivery-distribution

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a ticket delivery and distribution system with admin monitoring. Create a feature folder structure under src/features/ticket-delivery/ with the following organization:

Frontend side: Create folders for pages (TicketStatusPage, DeliverySettingsPage, AdminDeliveryDashboardPage), components (reusable UI pieces like status badges, delivery method cards, timeline components, metric cards), hooks (custom React hooks for fetching delivery status, managing preferences, polling for updates), types (TypeScript interfaces for delivery events and preferences), and services (API client functions).

Backend side (Laravel): Create a folder structure under app/Http/Controllers/DeliveryController.php, app/Models/DeliveryEvent.php, app/Models/DeliveryPreference.php, app/Policies/DeliveryEventPolicy.php, app/Policies/DeliveryPreferencePolicy.php, app/Jobs/SendTicketDeliveryJob.php, app/Jobs/RetryFailedDeliveryJob.php, database/migrations/ for the two new tables, and routes/api.php entries for the delivery endpoints.

This structure keeps delivery logic isolated and makes it easy to add new delivery methods or admin features later without touching other parts of the codebase.

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] Frontend folders exist: pages/, components/, hooks/, types/, services/ under src/features/ticket-delivery/
- [ ] Backend folders exist: Controllers/, Models/, Policies/, Jobs/ with the delivery-related files
- [ ] Database migrations folder is ready for new tables
- [ ] The project builds without errors after adding these folders

Test the actual flow: Run the build command (npm run build for frontend, php artisan serve for backend) and verify no errors appear.

Then give me your honest advice on:
- Whether this folder structure scales well if we add more delivery methods later
- If there's a better way to organize the backend controllers and jobs
- Whether the separation between frontend and backend is clear enough
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: