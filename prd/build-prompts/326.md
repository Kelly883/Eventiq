# Step 326 of 345: Test the event creation feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_event-creation-setup

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the event feature doesn't break and works as expected.

Unit tests:
- Event form validation: title required, start < end, capacity > 0, ticket tier price >= 0, etc.
- Error message mapper: each error code maps to the right user-friendly message
- Date/time formatting: dates display correctly in the UI

Component tests:
- OrganizerEventListPage: loading state shows skeleton rows, empty state shows illustration, tab switching filters events, search filters on client side, delete shows confirmation modal
- EventCreatePage: form renders all sections (Details, Venue, Tickets, Media), adding/removing ticket tiers works, file upload shows preview, validation errors appear inline, 'Publish Event' disables form and shows spinner
- EventEditPage: event data pre-fills all fields, banner preview shows current image, 'Save Changes' updates and redirects
- TicketTierForm: adding/removing tiers works, validation errors appear

Integration tests (full user flows):
- Create event: navigate to /organizer/events/create → fill form (title, description, dates, venue, 2 ticket tiers, upload banner) → click 'Publish Event' → success toast → land on /organizer/events → see new event in 'Upcoming' tab
- Edit event: navigate to /organizer/events → click 'Edit' on an event → form pre-fills → change title → click 'Save Changes' → success toast → event list shows updated title
- Delete event: navigate to /organizer/events → click 'Delete' on an event → confirm → event disappears from list
- Filter by tab: navigate to /organizer/events → click 'Past' tab → events filter to past status → click 'Upcoming' → events revert
- Search: navigate to /organizer/events → type in search → list filters by title
- Error handling: try creating event with invalid data → validation errors appear → fix and retry → succeeds

API endpoint tests:
- POST /api/organizer/events: valid data creates event, invalid data returns 422, missing auth returns 401
- GET /api/organizer/events: returns user's events only, filters by status, paginated
- GET /api/organizer/events/:eventId: returns full event with ticket tiers, 404 if not found or not owned
- PATCH /api/organizer/events/:eventId: updates event and ticket tiers atomically, 404 if not found, 403 if not owned
- DELETE /api/organizer/events/:eventId: soft-deletes event, 404 if not found, 403 if not owned
- POST /api/organizer/events/:eventId/upload-banner: uploads file, validates type/size, 422 if invalid

---

## Referenced Entities

All specifications needed for this task are embedded below.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (form validation, error mapper, date formatting)
- [ ] All component tests pass (OrganizerEventListPage, EventCreatePage, EventEditPage, TicketTierForm)
- [ ] All integration tests pass on a real device/environment
- [ ] All API endpoint tests pass (valid data, invalid data, auth, ownership)
- [ ] Test coverage is decent (pages, forms, state management, endpoints should be well-covered)

Test the actual flow: Run the full test suite. Then run the integration tests: create event → success → list shows it → edit it → success → list shows updated → delete it → success → list doesn't show it. Test error cases: create with invalid data → validation errors → fix → retry → succeeds. Test auth: call endpoints without token → 401. Call endpoints
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: