# OrganizerEventListPage

Route: /organizer/events

> **Design System:** When implementing this page, ensure it follows the design system
> specifications in `design-system.md` for colors, typography, spacing, and component patterns.

## Description

FUNCTIONALITY: A dashboard for authenticated organizers to view and manage their events. Displays a list of events in a table or card layout, separated by tabs for 'Upcoming', 'Past', and 'Drafts'. Each event item shows key details like title, date, and ticket sales progress. Provides a primary call-to-action button to 'Create New Event', which navigates to /organizer/events/create. Each event in the list has actions to 'Edit' (navigates to /organizer/events/:eventId/edit) and 'Delete' (triggers a confirmation modal).
DESIGN APPROACH: A data-dense layout for efficient management. A prominent 'Create New Event' button is always visible. Tabs provide quick filtering. A search bar allows filtering events by title. The list uses infinite scroll or pagination for performance. The design will use the organizer's branding colors from their profile for consistency.
STATES: Loading shows a skeleton table with 5 shimmering rows. Empty state displays an illustration with the message 'You haven't created any events yet.' and a 'Create New Event' button. Error 401 redirects to the login modal. Error 403 shows an 'Access Denied' page. Error 500 shows an error banner with a retry button.
SECURITY: Protected route for users with the 'organizer' role. API calls are scoped to the authenticated user.

## Additional Details

Endpoint Ids:
  1. event-creation-setup_endpoint_list_events
Requires Auth: false
Flow Ids:
  1. organizer-setup-event-creation
Order: 0
Ui Elements:
Schema Ids:
  1. events
  2. tickettiers

---
Generated by VisualPRD