# Checkout Endpoint Create Intent

POST /api/checkout/create-payment-intent

## Description

Creates a payment intent with a payment provider (e.g., Stripe). It first creates a pending 'orders' record, then requests the payment intent from the provider with the final calculated amount. It returns the `client_secret` to the frontend, which is used to securely initialize the payment UI.

---
Generated by VisualPRD