# Pricing Endpoint Delete Window

DELETE /api/organizer/events/:eventId/pricing/windows/:windowId

## Description

REQUEST: URL params eventId (UUID), windowId (UUID)
PROCESS: 1. Verify auth token 2. Query pricing_windows by windowId 3. Verify window belongs to eventId 4. Query events by eventId 5. Check user owns event via Policy 6. Soft-delete pricing_windows record (set deletedAt timestamp) 7. Return success
SUCCESS (200): { message: 'Pricing window deleted successfully' }
ERRORS: 400 (invalid IDs), 401 (unauthenticated), 403 (user doesn't own event), 404 (window or event not found), 429 (rate limited), 500 (database error)
SECURITY: Auth required. Event ownership enforced. Soft delete preserves sales history. Rate limit 10 deletes/min per user.

---
Generated by VisualPRD