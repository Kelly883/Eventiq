# Analytics Endpoint Sales Velocity

GET /api/organizer/events/:eventId/analytics/sales-velocity

## Description

REQUEST: URL param eventId (UUID), query params: ?granularity=hour|day|week (optional, default: hour), ?startDate=ISO8601 (optional), ?endDate=ISO8601 (optional)
PROCESS: 1. Verify auth token 2. Query events by eventId 3. Check user owns event via Policy 4. Query analytics_sales_timeline filtered by eventId and date range 5. Group sales by granularity (hour/day/week) 6. Calculate cumulative sales and hourly/daily sales count 7. Return time-series data for chart visualization
SUCCESS (200): { dataPoints: [{timestamp (ISO8601), cumulativeSales (integer), cumulativeRevenue (decimal), periodSales (integer), periodRevenue (decimal)}], granularity (string), startDate (ISO8601), endDate (ISO8601) }
ERRORS: 400 (invalid granularity or date range), 401 (unauthenticated), 403 (forbidden), 404 (event not found), 500 (database error)
SECURITY: Auth required. Event ownership enforced. Rate limit 20/min per user.

---
Generated by VisualPRD