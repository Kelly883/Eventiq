# Organizer Endpoint Update Profile

PATCH /api/organizers/me/update

## Description

REQUEST: { displayName (string, required), bio (string, max 500), email (string), phone (string), website (string), socialLinks (object with twitter/instagram/linkedin/youtube), brandingColors (object with primaryColor/accentColor hex), isPublic (boolean), emailPublic (boolean), phonePublic (boolean) }
PROCESS: 1. Verify auth token 2. Extract userId 3. Query organizers by userId 4. Validate input (displayName required, bio max 500, URLs valid format, hex colors valid) 5. Store old values for audit log 6. Update organizers record with new values 7. Create auditLog entry with adminId=userId, targetUserId=userId, action='profile_updated', oldValue/newValue objects 8. Return updated profile
SUCCESS (200): { id, displayName, bio, email, phone, website, socialLinks, brandingColors, isPublic, emailPublic, phonePublic, updatedAt }
ERRORS:
- 400: Validation failed (displayName required, bio too long, invalid URL format, invalid hex color)
- 401: User not authenticated
- 403: User does not have Organizer role
- 404: Organizer profile not found
- 429: Rate limit exceeded (5 updates/min per user)
- 500: Database error
SECURITY: Auth required. Organizer role enforced. Rate limit 5/min per user to prevent spam. All changes logged to auditLogs.

---
Generated by VisualPRD