# Api Integrations Endpoint Public Graphql

POST /graphql

## Description

Single endpoint for all GraphQL queries and mutations. Uses a library like `rebing/graphql-laravel` to handle request parsing and execution. Authentication is handled by the same API key middleware as the REST API, and scopes are checked at the resolver level for each query/mutation.

---
Generated by VisualPRD