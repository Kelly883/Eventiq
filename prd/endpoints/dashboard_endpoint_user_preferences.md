# Dashboard Endpoint User Preferences

GET /api/users/me/dashboard-preferences

## Description

REQUEST: No body, auth token in Authorization header
PROCESS: 1. Verify auth token and extract userId 2. Query user_dashboard_preferences by userId 3. If not found, return default preferences 4. Return preferences
SUCCESS (200): { defaultTicketFilter (string), defaultDateRange (string), showRecommendations (boolean), showActivityFeed (boolean), autoRefreshEnabled (boolean) }
ERRORS: 401 (user not authenticated), 403 (user does not have permission), 500 (database error)
SECURITY: Auth required (bearer token). User can only view their own preferences (enforced by Policy). Rate limit 20/min per user to prevent abuse.

---
Generated by VisualPRD