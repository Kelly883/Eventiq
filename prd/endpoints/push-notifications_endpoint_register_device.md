# Push Notifications Endpoint Register Device

POST /api/notifications/device-tokens

## Description

Registers or updates a device token for the authenticated user. Called from the client after obtaining a token from a push notification service like FCM. Uses Laravel's updateOrCreate Eloquent method to handle both new registrations and updates for existing devices based on the unique token.

---
Generated by VisualPRD