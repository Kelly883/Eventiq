# Settlement Endpoint Organizer Payouts List

GET /api/organizer/payouts/list

## Description

REQUEST: Query params: ?page=1&limit=50&status=pending|calculated|approved|processing|completed|failed&startDate=ISO8601&endDate=ISO8601&sortBy=createdAt|payoutAmount&sortOrder=asc|desc
PROCESS: 1. Verify auth token and extract organizerId 2. Query payouts by organizerId filtered by status, date range 3. Eager load related settlement_policies for commission display 4. Sort by requested field 5. Paginate with limit/offset 6. Return paginated list with payout details and status badges
SUCCESS (200): { payouts: [{id, settlementPeriodStartDate, settlementPeriodEndDate, grossRevenue, platformCommissionAmount, refundsDeducted, payoutAmount, status, payoutMethod, completedAt}], total, page, limit }
ERRORS: 400 (invalid query params), 401 (unauthenticated - attempt token refresh → if fails, redirect to login), 403 (user not organizer), 500 (database error)
SECURITY: Auth required (bearer token). Organizer can only view their own payouts (enforced by Policy). Rate limit 20/min per user to prevent abuse.

---
Generated by VisualPRD