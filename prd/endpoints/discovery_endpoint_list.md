# Discovery Endpoint List

GET /api/events/public/list

## Description

REQUEST: Query params: ?page=1&limit=20&category=concerts&dateRange=upcoming&priceMin=0&priceMax=500&sortBy=date|price|popularity&sortOrder=asc|desc
PROCESS: 1. Validate query params (page >= 1, limit <= 50, valid category, valid dateRange) 2. Query events collection filtered by status='published' 3. Apply category filter if provided 4. Apply date range filter (upcoming = start_datetime >= NOW, this week = start_datetime between NOW and NOW+7days, custom = between provided dates) 5. Join with pricing_windows to filter by price range 6. Join with analytics_events_metrics for popularity sorting 7. Sort by requested field 8. Paginate with limit/offset 9. Eager load organizer details (name, avatar, branding colors) 10. Return paginated list with ticket availability computed from ticket_inventory
SUCCESS (200): { events: [{id, title, organizer: {id, name, avatarUrl, brandingColors}, date, venue, bannerUrl, startingPrice, ticketsAvailable, ticketsSold, trending}], total, page, limit }
ERRORS: 400 (invalid query params - invalid page, limit > 50, invalid category, invalid dateRange), 500 (database error)
SECURITY: Public endpoint (no auth required). Rate limit 30/min per IP to prevent scraping. Only returns published events. Organizer contact info not included.

---
Generated by VisualPRD