# Accessibility Endpoint Update Preferences

PATCH /api/users/me/accessibility-preferences/update

## Description

REQUEST: { fontSize (integer, optional, 12-24), highContrast (boolean, optional), screenReaderOptimized (boolean, optional), focusIndicatorEnhanced (boolean, optional), motionReduced (boolean, optional), lineHeight (decimal, optional, 1.0-2.0), letterSpacing (decimal, optional, 0-0.2), wordSpacing (decimal, optional, 0-0.2), colorBlindnessMode (string, optional, 'none'|'protanopia'|'deuteranopia'|'tritanopia') }. PROCESS: 1. Verify auth token and extract userId 2. Validate input ranges (fontSize 12-24, lineHeight 1.0-2.0, etc.) 3. Query accessibility_preferences by userId 4. If not found, create new record with defaults 5. Update only provided fields 6. Set updatedAt to current timestamp 7. Create auditLog entry: action='accessibility_preferences_updated', userId, changedFields 8. Return updated preferences. SUCCESS (200): { fontSize, highContrast, screenReaderOptimized, focusIndicatorEnhanced, motionReduced, lineHeight, letterSpacing, wordSpacing, colorBlindnessMode, message: 'Accessibility preferences updated' }. ERRORS: 400 (validation error - invalid range), 401 (unauthenticated), 403 (user doesn't have permission), 429 (rate limited), 500 (database error). SECURITY: Auth required (bearer token). User can only update their own preferences (enforced by Policy). Rate limit 10 changes/min per user to prevent spam. All changes logged to auditLogs.

---
Generated by VisualPRD