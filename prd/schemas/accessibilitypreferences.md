# accessibility_preferences

## Description

PURPOSE: Store user accessibility preferences per user, enabling WCAG 2

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship
│   ├── Constraints: required
│   └── Mutable: no

fontSize: integer
├── Purpose: Font size in pixels (12-24), defaults to 16
│   ├── Constraints: required
│   └── Mutable: yes

highContrast: boolean
├── Purpose: Whether to enable high contrast mode (WCAG AAA), defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

screenReaderOptimized: boolean
├── Purpose: Whether to enable screen reader optimizations (ARIA labels, semantic HTML), defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

focusIndicatorEnhanced: boolean
├── Purpose: Whether to enhance keyboard focus indicators for better visibility, defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

motionReduced: boolean
├── Purpose: Whether to disable animations and transitions (prefers-reduced-motion), defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

lineHeight: decimal(3,2)
├── Purpose: Line height multiplier (1.0-2.0), defaults to 1.5
│   ├── Constraints: required
│   └── Mutable: yes

letterSpacing: decimal(4,3)
├── Purpose: Letter spacing in em units (0-0.2), defaults to 0
│   ├── Constraints: required
│   └── Mutable: yes

wordSpacing: decimal(4,3)
├── Purpose: Word spacing in em units (0-0.2), defaults to 0
│   ├── Constraints: required
│   └── Mutable: yes

colorBlindnessMode: string
├── Purpose: Color blindness simulation mode: 'none', 'protanopia', 'deuteranopia', 'tritanopia', defaults to 'none'
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Preference creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last preference update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| userId | string | Yes | Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship |
| fontSize | integer | Yes | Font size in pixels (12-24), defaults to 16 |
| highContrast | boolean | Yes | Whether to enable high contrast mode (WCAG AAA), defaults to false |
| screenReaderOptimized | boolean | Yes | Whether to enable screen reader optimizations (ARIA labels, semantic HTML), defaults to false |
| focusIndicatorEnhanced | boolean | Yes | Whether to enhance keyboard focus indicators for better visibility, defaults to false |
| motionReduced | boolean | Yes | Whether to disable animations and transitions (prefers-reduced-motion), defaults to false |
| lineHeight | decimal(3,2) | Yes | Line height multiplier (1.0-2.0), defaults to 1.5 |
| letterSpacing | decimal(4,3) | Yes | Letter spacing in em units (0-0.2), defaults to 0 |
| wordSpacing | decimal(4,3) | Yes | Word spacing in em units (0-0.2), defaults to 0 |
| colorBlindnessMode | string | No | Color blindness simulation mode: 'none', 'protanopia', 'deuteranopia', 'tritanopia', defaults to 'none' |
| createdAt | timestamp | Yes | Preference creation timestamp |
| updatedAt | timestamp | Yes | Last preference update timestamp |

## Relationships


## Indexes


---
Generated by VisualPRD