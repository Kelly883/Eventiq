# push_notification_devices

## Description

PURPOSE: Stores the mapping of users to their device tokens for push notifications

FIELDS:
id:string (PK): string
├── Purpose: Id:string ( p k) value
│   ├── Constraints: required
│   └── Mutable: yes

user_id:string (FK to users.id): array
├── Purpose: User id:string ( f k to users.id) value
│   ├── Constraints: required
│   └── Mutable: yes

token:string (unique): string
├── Purpose: Token:string (unique) value
│   └── Mutable: yes

provider:string (e.g., 'fcm'): string
├── Purpose: Provider:string (e.g., 'fcm') value
│   ├── Constraints: required
│   └── Mutable: yes

device_type:string (enum: 'web', 'ios', 'android'): string
├── Purpose: Device type:string (enum: 'web', 'ios', 'android') value
│   ├── Constraints: required
│   └── Mutable: yes

created_at:timestamp: string
├── Purpose: Created at:timestamp value
│   └── Mutable: yes

updated_at:timestamp: timestamp
└── Purpose: Updated at:timestamp value
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| unnamed | string | No |  |
| unnamed | string | No |  |
| unnamed | string | No |  |
| unnamed | string | No |  |
| unnamed | string | No |  |
| unnamed | string | No |  |
| unnamed | string | No |  |

## Relationships


## Indexes


---
Generated by VisualPRD