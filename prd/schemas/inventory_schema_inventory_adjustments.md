# inventory_adjustments

## Description

PURPOSE: Immutable audit trail of all manual inventory adjustments, tracking who changed what and when

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

eventId: string
├── Purpose: Foreign key reference to events.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

ticketTierId: string
├── Purpose: Foreign key reference to ticket_tiers.id
│   ├── Constraints: required
│   └── Mutable: no

pricingWindowId: string
├── Purpose: Optional foreign key reference to pricing_windows.id if adjustment is for a specific window, null if adjustment is for entire tier
│   └── Mutable: no

organizerId: string
├── Purpose: Foreign key reference to organizers.id — organizer who made the adjustment
│   ├── Constraints: required
│   └── Mutable: no

adjustmentType: string
├── Purpose: Type of adjustment: 'manual_increase', 'manual_decrease', 'reallocation', 'system_correction' (for automated fixes)
│   ├── Constraints: required
│   └── Mutable: yes

quantityBefore: integer
├── Purpose: Inventory quantity before adjustment
│   ├── Constraints: required
│   └── Mutable: yes

quantityAfter: integer
├── Purpose: Inventory quantity after adjustment
│   ├── Constraints: required
│   └── Mutable: yes

quantityDelta: integer
├── Purpose: Computed: quantityAfter - quantityBefore (positive for increase, negative for decrease)
│   ├── Constraints: required
│   └── Mutable: yes

reason: string
├── Purpose: Optional reason provided by organizer for the adjustment (e.g., 'Venue capacity increased', 'Compensate for no-shows')
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Adjustment timestamp
    ├── Constraints: required
    └── Mutable: no

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| eventId | string | Yes | Foreign key reference to events.id, indexed for fast lookups |
| ticketTierId | string | Yes | Foreign key reference to ticket_tiers.id |
| pricingWindowId | string | No | Optional foreign key reference to pricing_windows.id if adjustment is for a specific window, null if adjustment is for entire tier |
| organizerId | string | Yes | Foreign key reference to organizers.id — organizer who made the adjustment |
| adjustmentType | string | Yes | Type of adjustment: 'manual_increase', 'manual_decrease', 'reallocation', 'system_correction' (for automated fixes) |
| quantityBefore | integer | Yes | Inventory quantity before adjustment |
| quantityAfter | integer | Yes | Inventory quantity after adjustment |
| quantityDelta | integer | Yes | Computed: quantityAfter - quantityBefore (positive for increase, negative for decrease) |
| reason | string | No | Optional reason provided by organizer for the adjustment (e.g., 'Venue capacity increased', 'Compensate for no-shows') |
| createdAt | timestamp | Yes | Adjustment timestamp |

---
Generated by VisualPRD