# users

## Description

PURPOSE: Core user accounts table for authentication and profile management

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

email: string
├── Purpose: Unique email address, indexed for fast lookups, used for login and password reset
│   ├── Constraints: required
│   └── Mutable: yes

passwordHash: string
├── Purpose: Bcrypt hashed password (cost 12), never exposed in API responses
│   ├── Constraints: required
│   └── Mutable: yes

name: string
├── Purpose: User's display name, can be updated in profile
│   ├── Constraints: required
│   └── Mutable: yes

role: string
├── Purpose: User role: 'organizer' or 'attendee', determines feature access
│   ├── Constraints: required
│   └── Mutable: yes

emailVerified: boolean
├── Purpose: Whether email has been verified, defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Account creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
├── Purpose: Last profile update timestamp
│   ├── Constraints: required
│   └── Mutable: yes

lastLoginAt: timestamp
└── Purpose: Last successful login timestamp, used for security audits
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| email | string | Yes | Unique email address, indexed for fast lookups, used for login and password reset |
| passwordHash | string | Yes | Bcrypt hashed password (cost 12), never exposed in API responses |
| name | string | Yes | User's display name, can be updated in profile |
| role | string | Yes | User role: 'organizer' or 'attendee', determines feature access |
| emailVerified | boolean | Yes | Whether email has been verified, defaults to false |
| createdAt | timestamp | Yes | Account creation timestamp |
| updatedAt | timestamp | Yes | Last profile update timestamp |
| lastLoginAt | timestamp | No | Last successful login timestamp, used for security audits |

## Relationships


## Indexes


---
Generated by VisualPRD