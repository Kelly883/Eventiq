# organizer_dashboard_preferences

## Description

PURPOSE: Store organizer dashboard UI preferences and state (filters, expanded sections, refresh settings)

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

organizerId: string
├── Purpose: Foreign key reference to organizers.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

defaultEventFilter: string
├── Purpose: Default event status filter: 'all', 'upcoming', 'past', 'draft', defaults to 'all'
│   └── Mutable: yes

defaultDateRange: string
├── Purpose: Default date range for metrics: '7days', '30days', '90days', 'all', defaults to '30days'
│   └── Mutable: yes

expandedEventId: string
├── Purpose: ID of currently expanded event detail widget, null if none expanded
│   └── Mutable: no

showActivityFeed: boolean
├── Purpose: Whether to show sales activity feed section, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

autoRefreshEnabled: boolean
├── Purpose: Whether to auto-refresh metrics every 30 seconds, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Preference creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last preference update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| organizerId | string | Yes | Foreign key reference to organizers.id, indexed for fast lookups |
| defaultEventFilter | string | No | Default event status filter: 'all', 'upcoming', 'past', 'draft', defaults to 'all' |
| defaultDateRange | string | No | Default date range for metrics: '7days', '30days', '90days', 'all', defaults to '30days' |
| expandedEventId | string | No | ID of currently expanded event detail widget, null if none expanded |
| showActivityFeed | boolean | Yes | Whether to show sales activity feed section, defaults to true |
| autoRefreshEnabled | boolean | Yes | Whether to auto-refresh metrics every 30 seconds, defaults to true |
| createdAt | timestamp | Yes | Preference creation timestamp |
| updatedAt | timestamp | Yes | Last preference update timestamp |

---
Generated by VisualPRD