# delivery_preferences

## Description

PURPOSE: Store user delivery preferences and settings, controlling how and when tickets are delivered

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship
│   ├── Constraints: required
│   └── Mutable: no

emailEnabled: boolean
├── Purpose: Whether email delivery is enabled, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

emailAddress: string
├── Purpose: Email address for ticket delivery, defaults to users.email, can be overridden
│   └── Mutable: yes

emailVerified: boolean
├── Purpose: Whether email address has been verified, defaults to false, must be true to enable delivery
│   ├── Constraints: required
│   └── Mutable: yes

smsEnabled: boolean
├── Purpose: Whether SMS delivery is enabled, defaults to false (opt-in)
│   ├── Constraints: required
│   └── Mutable: yes

phoneNumber: string
├── Purpose: Phone number for SMS delivery, stored in E.164 format, null if SMS not enabled
│   └── Mutable: yes

phoneVerified: boolean
├── Purpose: Whether phone number has been verified, defaults to false, must be true to enable SMS delivery
│   ├── Constraints: required
│   └── Mutable: yes

dashboardEnabled: boolean
├── Purpose: Whether dashboard delivery is enabled (always available), defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

primaryMethod: string
├── Purpose: Primary delivery method: 'email', 'sms', or 'dashboard', defaults to 'email'
│   ├── Constraints: required
│   └── Mutable: yes

backupMethod: string
├── Purpose: Backup delivery method if primary fails: 'email', 'sms', or 'dashboard', null if no backup
│   └── Mutable: yes

deliveryTiming: string
├── Purpose: When to deliver tickets: 'immediate' (default), 'scheduled_1h_before', 'scheduled_24h_before'
│   ├── Constraints: required
│   └── Mutable: yes

receiveConfirmation: boolean
├── Purpose: Whether to receive delivery confirmation notification, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

receiveReminders: boolean
├── Purpose: Whether to receive event reminders before event date, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

languagePreference: string
├── Purpose: Language for delivery emails: 'en', 'es', 'fr', 'de', defaults to 'en'
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Preference creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last preference update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| userId | string | Yes | Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship |
| emailEnabled | boolean | Yes | Whether email delivery is enabled, defaults to true |
| emailAddress | string | No | Email address for ticket delivery, defaults to users.email, can be overridden |
| emailVerified | boolean | Yes | Whether email address has been verified, defaults to false, must be true to enable delivery |
| smsEnabled | boolean | Yes | Whether SMS delivery is enabled, defaults to false (opt-in) |
| phoneNumber | string | No | Phone number for SMS delivery, stored in E.164 format, null if SMS not enabled |
| phoneVerified | boolean | Yes | Whether phone number has been verified, defaults to false, must be true to enable SMS delivery |
| dashboardEnabled | boolean | Yes | Whether dashboard delivery is enabled (always available), defaults to true |
| primaryMethod | string | Yes | Primary delivery method: 'email', 'sms', or 'dashboard', defaults to 'email' |
| backupMethod | string | No | Backup delivery method if primary fails: 'email', 'sms', or 'dashboard', null if no backup |
| deliveryTiming | string | Yes | When to deliver tickets: 'immediate' (default), 'scheduled_1h_before', 'scheduled_24h_before' |
| receiveConfirmation | boolean | Yes | Whether to receive delivery confirmation notification, defaults to true |
| receiveReminders | boolean | Yes | Whether to receive event reminders before event date, defaults to true |
| languagePreference | string | Yes | Language for delivery emails: 'en', 'es', 'fr', 'de', defaults to 'en' |
| createdAt | timestamp | Yes | Preference creation timestamp |
| updatedAt | timestamp | Yes | Last preference update timestamp |

## Relationships


## Indexes


---
Generated by VisualPRD