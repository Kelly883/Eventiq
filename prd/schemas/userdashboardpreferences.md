# user_dashboard_preferences

## Description

PURPOSE: Store user dashboard UI preferences and state (filters, display options, refresh settings)

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship
│   ├── Constraints: required
│   └── Mutable: no

defaultTicketFilter: string
├── Purpose: Default ticket filter: 'all', 'upcoming', 'past', defaults to 'all'
│   └── Mutable: yes

defaultDateRange: string
├── Purpose: Default date range for metrics: '7days', '30days', '90days', 'all', defaults to '30days'
│   └── Mutable: yes

showRecommendations: boolean
├── Purpose: Whether to show personalized event recommendations on dashboard, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

showActivityFeed: boolean
├── Purpose: Whether to show recent activity feed section, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

autoRefreshEnabled: boolean
├── Purpose: Whether to auto-refresh dashboard every 30 seconds, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Preference creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last preference update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| userId | string | Yes | Foreign key reference to users.id, indexed for fast lookups, one-to-one relationship |
| defaultTicketFilter | string | No | Default ticket filter: 'all', 'upcoming', 'past', defaults to 'all' |
| defaultDateRange | string | No | Default date range for metrics: '7days', '30days', '90days', 'all', defaults to '30days' |
| showRecommendations | boolean | Yes | Whether to show personalized event recommendations on dashboard, defaults to true |
| showActivityFeed | boolean | Yes | Whether to show recent activity feed section, defaults to true |
| autoRefreshEnabled | boolean | Yes | Whether to auto-refresh dashboard every 30 seconds, defaults to true |
| createdAt | timestamp | Yes | Preference creation timestamp |
| updatedAt | timestamp | Yes | Last preference update timestamp |

## Relationships


## Indexes


---
Generated by VisualPRD