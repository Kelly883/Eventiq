# settlement_policies

## Description

PURPOSE: Store settlement policies defining commission rates, payout frequency, and processing rules for different organizer tiers

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

organizerTier: string
├── Purpose: Organizer tier: 'standard', 'premium', 'enterprise', determines commission rates and payout frequency
│   ├── Constraints: required
│   └── Mutable: yes

platformCommissionPercentage: decimal(5, 2)
├── Purpose: Platform commission percentage (e.g., 10 for 10%), varies by tier
│   ├── Constraints: required
│   └── Mutable: yes

processingFeePercentage: decimal(5, 2)
├── Purpose: Payment processor fee percentage (e.g., 2.9 for Stripe), passed through to organizer
│   ├── Constraints: required
│   └── Mutable: yes

payoutFrequency: string
├── Purpose: Payout frequency: 'daily', 'weekly', 'biweekly', 'monthly', 'on_demand'
│   ├── Constraints: required
│   └── Mutable: yes

minimumPayoutThreshold: decimal(10, 2)
├── Purpose: Minimum payout amount (e.g., $100) — payouts below this are held until next period
│   ├── Constraints: required
│   └── Mutable: yes

payoutHoldDays: integer
├── Purpose: Number of days to hold payout after settlement period ends (e.g., 3 days for fraud review)
│   ├── Constraints: required
│   └── Mutable: yes

requiresApproval: boolean
├── Purpose: Whether payouts require manual admin approval before processing, defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

autoApproveThreshold: decimal(10, 2)
├── Purpose: Optional: Auto-approve payouts below this amount without admin review
│   └── Mutable: yes

maxRetries: integer
├── Purpose: Maximum retry attempts for failed payouts, defaults to 3
│   ├── Constraints: required
│   └── Mutable: yes

retryBackoffMultiplier: decimal(3, 2)
├── Purpose: Exponential backoff multiplier for retries (e.g., 2.0 means double wait time each retry)
│   ├── Constraints: required
│   └── Mutable: yes

taxWithholdingPercentage: decimal(5, 2)
├── Purpose: Optional tax withholding percentage for 1099 contractors (e.g., 30)
│   └── Mutable: yes

allowedPayoutMethods: array<string>
├── Purpose: Array of allowed payout methods: ['ach', 'wire_transfer', 'check', 'store_credit']
│   ├── Constraints: required
│   └── Mutable: yes

isActive: boolean
├── Purpose: Whether this policy is currently active, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Policy creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last policy update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| organizerTier | string | Yes | Organizer tier: 'standard', 'premium', 'enterprise', determines commission rates and payout frequency |
| platformCommissionPercentage | decimal(5, 2) | Yes | Platform commission percentage (e.g., 10 for 10%), varies by tier |
| processingFeePercentage | decimal(5, 2) | Yes | Payment processor fee percentage (e.g., 2.9 for Stripe), passed through to organizer |
| payoutFrequency | string | Yes | Payout frequency: 'daily', 'weekly', 'biweekly', 'monthly', 'on_demand' |
| minimumPayoutThreshold | decimal(10, 2) | Yes | Minimum payout amount (e.g., $100) — payouts below this are held until next period |
| payoutHoldDays | integer | Yes | Number of days to hold payout after settlement period ends (e.g., 3 days for fraud review) |
| requiresApproval | boolean | Yes | Whether payouts require manual admin approval before processing, defaults to false |
| autoApproveThreshold | decimal(10, 2) | No | Optional: Auto-approve payouts below this amount without admin review |
| maxRetries | integer | Yes | Maximum retry attempts for failed payouts, defaults to 3 |
| retryBackoffMultiplier | decimal(3, 2) | Yes | Exponential backoff multiplier for retries (e.g., 2.0 means double wait time each retry) |
| taxWithholdingPercentage | decimal(5, 2) | No | Optional tax withholding percentage for 1099 contractors (e.g., 30) |
| allowedPayoutMethods | array<string> | Yes | Array of allowed payout methods: ['ach', 'wire_transfer', 'check', 'store_credit'] |
| isActive | boolean | Yes | Whether this policy is currently active, defaults to true |
| createdAt | timestamp | Yes | Policy creation timestamp |
| updatedAt | timestamp | Yes | Last policy update timestamp |

---
Generated by VisualPRD