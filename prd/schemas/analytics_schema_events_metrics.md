# analytics_events_metrics

## Description

PURPOSE: Aggregate real-time sales metrics per event, denormalizing key performance indicators for fast dashboard queries

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

eventId: string
├── Purpose: Foreign key reference to events.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

organizerId: string
├── Purpose: Foreign key reference to organizers.id for access control
│   ├── Constraints: required
│   └── Mutable: no

totalRevenue: decimal(12, 2)
├── Purpose: Denormalized total revenue from all ticket sales, updated on each purchase
│   ├── Constraints: required
│   └── Mutable: yes

totalTicketsSold: integer
├── Purpose: Denormalized count of tickets sold, updated on each purchase
│   ├── Constraints: required
│   └── Mutable: yes

totalPageViews: integer
├── Purpose: Total event page views tracked via Mixpanel, updated hourly
│   ├── Constraints: required
│   └── Mutable: yes

totalTicketPageViews: integer
├── Purpose: Total ticket purchase page views, updated hourly
│   ├── Constraints: required
│   └── Mutable: yes

conversionRate: decimal(5, 2)
├── Purpose: Computed: (totalTicketsSold / totalPageViews) * 100, percentage value
│   ├── Constraints: required
│   └── Mutable: yes

averageTicketPrice: decimal(10, 2)
├── Purpose: Computed: totalRevenue / totalTicketsSold, updated on each purchase
│   ├── Constraints: required
│   └── Mutable: yes

peakSalesHour: integer
├── Purpose: Hour of day (0-23) with highest sales volume, computed daily
│   └── Mutable: yes

topTicketTier: string
├── Purpose: ID of ticket tier with highest sales volume, computed hourly
│   └── Mutable: yes

lastUpdatedAt: timestamp
├── Purpose: Timestamp of last metric update, used for real-time indicator
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Metrics record creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last metrics update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| eventId | string | Yes | Foreign key reference to events.id, indexed for fast lookups |
| organizerId | string | Yes | Foreign key reference to organizers.id for access control |
| totalRevenue | decimal(12, 2) | Yes | Denormalized total revenue from all ticket sales, updated on each purchase |
| totalTicketsSold | integer | Yes | Denormalized count of tickets sold, updated on each purchase |
| totalPageViews | integer | Yes | Total event page views tracked via Mixpanel, updated hourly |
| totalTicketPageViews | integer | Yes | Total ticket purchase page views, updated hourly |
| conversionRate | decimal(5, 2) | Yes | Computed: (totalTicketsSold / totalPageViews) * 100, percentage value |
| averageTicketPrice | decimal(10, 2) | Yes | Computed: totalRevenue / totalTicketsSold, updated on each purchase |
| peakSalesHour | integer | No | Hour of day (0-23) with highest sales volume, computed daily |
| topTicketTier | string | No | ID of ticket tier with highest sales volume, computed hourly |
| lastUpdatedAt | timestamp | Yes | Timestamp of last metric update, used for real-time indicator |
| createdAt | timestamp | Yes | Metrics record creation timestamp |
| updatedAt | timestamp | Yes | Last metrics update timestamp |

---
Generated by VisualPRD