# admin_settings

## Description

PURPOSE: Centralized platform configuration for admin-controlled settings (fraud thresholds, commission rates, payout rules, notification settings, system parameters)

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

settingKey: string
├── Purpose: Unique setting identifier (e.g., 'platform_commission_default', 'fraud_score_threshold', 'payout_hold_days')
│   ├── Constraints: required
│   └── Mutable: yes

settingValue: string|number|boolean|object
├── Purpose: Setting value, type varies by key
│   ├── Constraints: required
│   └── Mutable: yes

description: string
├── Purpose: Human-readable description of what this setting controls
│   └── Mutable: yes

category: string
├── Purpose: Setting category: 'platform', 'fraud', 'payments', 'notifications', 'system'
│   ├── Constraints: required
│   └── Mutable: yes

isEditable: boolean
├── Purpose: Whether this setting can be edited by admins, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

lastModifiedBy: string
├── Purpose: Foreign key to users.id — admin who last modified this setting
│   └── Mutable: yes

lastModifiedAt: timestamp
├── Purpose: Timestamp of last modification
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Setting creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last setting update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| settingKey | string | Yes | Unique setting identifier (e.g., 'platform_commission_default', 'fraud_score_threshold', 'payout_hold_days') |
| settingValue | string|number|boolean|object | Yes | Setting value, type varies by key |
| description | string | No | Human-readable description of what this setting controls |
| category | string | Yes | Setting category: 'platform', 'fraud', 'payments', 'notifications', 'system' |
| isEditable | boolean | Yes | Whether this setting can be edited by admins, defaults to true |
| lastModifiedBy | string | No | Foreign key to users.id — admin who last modified this setting |
| lastModifiedAt | timestamp | No | Timestamp of last modification |
| createdAt | timestamp | Yes | Setting creation timestamp |
| updatedAt | timestamp | Yes | Last setting update timestamp |

---
Generated by VisualPRD