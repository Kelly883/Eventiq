# sessions

## Description

PURPOSE: Session management table tracking active user sessions and tokens

FIELDS:
id: string
├── Purpose: UUID primary key
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id
│   ├── Constraints: required
│   └── Mutable: no

token: string
├── Purpose: JWT or session token, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: yes

expiresAt: timestamp
├── Purpose: Token expiration time, typically 7 days from creation
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Session creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

revokedAt: timestamp
└── Purpose: Timestamp when session was revoked (logout or password reset), null if active
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key |
| userId | string | Yes | Foreign key reference to users.id |
| token | string | Yes | JWT or session token, indexed for fast lookups |
| expiresAt | timestamp | Yes | Token expiration time, typically 7 days from creation |
| createdAt | timestamp | Yes | Session creation timestamp |
| revokedAt | timestamp | No | Timestamp when session was revoked (logout or password reset), null if active |

## Relationships


## Indexes


---
Generated by VisualPRD