# passwordResetTokens

## Description

PURPOSE: Password reset token management

FIELDS:
id: string
├── Purpose: UUID primary key
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id
│   ├── Constraints: required
│   └── Mutable: no

token: string
├── Purpose: Hashed reset token (not plain text), indexed for lookups
│   ├── Constraints: required
│   └── Mutable: yes

expiresAt: timestamp
├── Purpose: Token expiration time, typically 1 hour from creation
│   ├── Constraints: required
│   └── Mutable: yes

usedAt: timestamp
├── Purpose: Timestamp when token was used to reset password, null if unused. Prevents token reuse.
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Token creation timestamp
    ├── Constraints: required
    └── Mutable: no

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key |
| userId | string | Yes | Foreign key reference to users.id |
| token | string | Yes | Hashed reset token (not plain text), indexed for lookups |
| expiresAt | timestamp | Yes | Token expiration time, typically 1 hour from creation |
| usedAt | timestamp | No | Timestamp when token was used to reset password, null if unused. Prevents token reuse. |
| createdAt | timestamp | Yes | Token creation timestamp |

## Relationships


## Indexes


---
Generated by VisualPRD