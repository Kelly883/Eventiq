# refund_appeals

## Description

PURPOSE: Track refund decision appeals submitted by users who disagree with rejection

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

refundRequestId: string
├── Purpose: Foreign key reference to refund_requests.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id, indexed for user-level appeal tracking
│   ├── Constraints: required
│   └── Mutable: no

appealReason: text
├── Purpose: User's explanation for appealing the refund decision
│   ├── Constraints: required
│   └── Mutable: yes

status: string
├── Purpose: Appeal status: 'pending', 'approved', 'rejected', defaults to 'pending'
│   ├── Constraints: required
│   └── Mutable: yes

reviewedBy: string
├── Purpose: Foreign key to users.id — admin who reviewed the appeal, null if not yet reviewed
│   └── Mutable: yes

reviewNotes: text
├── Purpose: Optional notes from admin reviewer documenting the appeal decision
│   └── Mutable: yes

reviewedAt: timestamp
├── Purpose: Timestamp when appeal was reviewed by admin
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Appeal submission timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last appeal update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| refundRequestId | string | Yes | Foreign key reference to refund_requests.id, indexed for fast lookups |
| userId | string | Yes | Foreign key reference to users.id, indexed for user-level appeal tracking |
| appealReason | text | Yes | User's explanation for appealing the refund decision |
| status | string | Yes | Appeal status: 'pending', 'approved', 'rejected', defaults to 'pending' |
| reviewedBy | string | No | Foreign key to users.id — admin who reviewed the appeal, null if not yet reviewed |
| reviewNotes | text | No | Optional notes from admin reviewer documenting the appeal decision |
| reviewedAt | timestamp | No | Timestamp when appeal was reviewed by admin |
| createdAt | timestamp | Yes | Appeal submission timestamp |
| updatedAt | timestamp | Yes | Last appeal update timestamp |

## Relationships


## Indexes


---
Generated by VisualPRD