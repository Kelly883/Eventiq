# payout_calculations

## Description

PURPOSE: Immutable audit trail of all payout calculations, storing detailed breakdown of revenue, deductions, and commission calculations for every settlement

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

payoutId: string
├── Purpose: Foreign key reference to payouts.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

organizerId: string
├── Purpose: Foreign key reference to organizers.id, indexed for organizer-level tracking
│   ├── Constraints: required
│   └── Mutable: no

settlementPeriodStartDate: timestamp
├── Purpose: Start date of settlement period
│   ├── Constraints: required
│   └── Mutable: yes

settlementPeriodEndDate: timestamp
├── Purpose: End date of settlement period
│   ├── Constraints: required
│   └── Mutable: yes

eventIds: array<string>
├── Purpose: Array of event IDs included in this settlement period
│   ├── Constraints: required
│   └── Mutable: yes

orderIds: array<string>
├── Purpose: Array of order IDs included in this settlement (for audit trail)
│   ├── Constraints: required
│   └── Mutable: yes

refundRequestIds: array<string>
├── Purpose: Array of completed refund request IDs deducted from this settlement
│   ├── Constraints: required
│   └── Mutable: yes

totalOrderCount: integer
├── Purpose: Total number of orders in settlement period
│   ├── Constraints: required
│   └── Mutable: yes

totalTicketsSold: integer
├── Purpose: Total tickets sold in settlement period
│   ├── Constraints: required
│   └── Mutable: yes

totalRefundsProcessed: integer
├── Purpose: Total number of refunds completed in settlement period
│   ├── Constraints: required
│   └── Mutable: yes

calculationDetails: object
├── Purpose: Detailed calculation breakdown: {grossRevenue, refundsDeducted, netRevenue, commissionPercentage, commissionAmount, processingFeePercentage, processingFeeAmount, taxWithholdingPercentage, taxWithholdingAmount, payoutAmount}
│   ├── Constraints: required
│   └── Mutable: yes

calculatedAt: timestamp
├── Purpose: Timestamp when calculation was performed
│   ├── Constraints: required
│   └── Mutable: yes

calculatedBy: string
├── Purpose: System identifier or admin ID who triggered calculation (e.g., 'system_scheduled' or user ID)
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Record creation timestamp
    ├── Constraints: required
    └── Mutable: no

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| payoutId | string | Yes | Foreign key reference to payouts.id, indexed for fast lookups |
| organizerId | string | Yes | Foreign key reference to organizers.id, indexed for organizer-level tracking |
| settlementPeriodStartDate | timestamp | Yes | Start date of settlement period |
| settlementPeriodEndDate | timestamp | Yes | End date of settlement period |
| eventIds | array<string> | Yes | Array of event IDs included in this settlement period |
| orderIds | array<string> | Yes | Array of order IDs included in this settlement (for audit trail) |
| refundRequestIds | array<string> | Yes | Array of completed refund request IDs deducted from this settlement |
| totalOrderCount | integer | Yes | Total number of orders in settlement period |
| totalTicketsSold | integer | Yes | Total tickets sold in settlement period |
| totalRefundsProcessed | integer | Yes | Total number of refunds completed in settlement period |
| calculationDetails | object | Yes | Detailed calculation breakdown: {grossRevenue, refundsDeducted, netRevenue, commissionPercentage, commissionAmount, processingFeePercentage, processingFeeAmount, taxWithholdingPercentage, taxWithholdingAmount, payoutAmount} |
| calculatedAt | timestamp | Yes | Timestamp when calculation was performed |
| calculatedBy | string | Yes | System identifier or admin ID who triggered calculation (e.g., 'system_scheduled' or user ID) |
| createdAt | timestamp | Yes | Record creation timestamp |

---
Generated by VisualPRD