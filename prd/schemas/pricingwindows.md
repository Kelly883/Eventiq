# pricing_windows

## Description

PURPOSE: Store time-based pricing windows for ticket categories

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

eventId: string
├── Purpose: Foreign key reference to events.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

ticketCategoryId: string
├── Purpose: Foreign key reference to ticket_tiers.id, specifies which ticket category this pricing applies to
│   ├── Constraints: required
│   └── Mutable: no

windowName: string
├── Purpose: Human-readable name for this pricing window (e.g., 'Early Bird', 'Regular', 'Last Minute')
│   ├── Constraints: required
│   └── Mutable: yes

startDateTime: timestamp
├── Purpose: When this pricing window becomes active, indexed for range queries
│   ├── Constraints: required
│   └── Mutable: yes

endDateTime: timestamp
├── Purpose: When this pricing window expires, indexed for range queries
│   ├── Constraints: required
│   └── Mutable: yes

price: decimal(10, 2)
├── Purpose: Price per ticket during this window, overrides base ticket_tiers.price
│   ├── Constraints: required
│   └── Mutable: yes

quantityLimit: integer
├── Purpose: Optional maximum tickets available at this price, null means unlimited
│   └── Mutable: yes

quantitySold: integer
├── Purpose: Denormalized count of tickets sold at this price, updated on each purchase
│   ├── Constraints: required
│   └── Mutable: yes

isActive: boolean
├── Purpose: Whether this window is currently active (computed based on current time vs startDateTime/endDateTime)
│   ├── Constraints: required
│   └── Mutable: yes

priority: integer
├── Purpose: Sort order for windows (lower number = higher priority if overlapping, though overlaps should be prevented)
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Window creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
├── Purpose: Last window update timestamp
│   ├── Constraints: required
│   └── Mutable: yes

deletedAt: timestamp
└── Purpose: Soft delete timestamp, null if active
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| eventId | string | Yes | Foreign key reference to events.id, indexed for fast lookups |
| ticketCategoryId | string | Yes | Foreign key reference to ticket_tiers.id, specifies which ticket category this pricing applies to |
| windowName | string | Yes | Human-readable name for this pricing window (e.g., 'Early Bird', 'Regular', 'Last Minute') |
| startDateTime | timestamp | Yes | When this pricing window becomes active, indexed for range queries |
| endDateTime | timestamp | Yes | When this pricing window expires, indexed for range queries |
| price | decimal(10, 2) | Yes | Price per ticket during this window, overrides base ticket_tiers.price |
| quantityLimit | integer | No | Optional maximum tickets available at this price, null means unlimited |
| quantitySold | integer | Yes | Denormalized count of tickets sold at this price, updated on each purchase |
| isActive | boolean | Yes | Whether this window is currently active (computed based on current time vs startDateTime/endDateTime) |
| priority | integer | Yes | Sort order for windows (lower number = higher priority if overlapping, though overlaps should be prevented) |
| createdAt | timestamp | Yes | Window creation timestamp |
| updatedAt | timestamp | Yes | Last window update timestamp |
| deletedAt | timestamp | No | Soft delete timestamp, null if active |

## Relationships


## Indexes


---
Generated by VisualPRD