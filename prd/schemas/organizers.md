# organizers

## Description

PURPOSE: Store organizer profile data including branding, contact info, and public profile settings

FIELDS:
id: string
├── Purpose: UUID primary key, auto-generated
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: Foreign key reference to users.id, indexed for fast lookups
│   ├── Constraints: required
│   └── Mutable: no

displayName: string
├── Purpose: Organizer's public display name, can differ from user.name
│   ├── Constraints: required
│   └── Mutable: yes

bio: string
├── Purpose: Organizer bio/description, max 500 characters
│   └── Mutable: yes

avatarUrl: string
├── Purpose: URL to organizer avatar image (stored in S3/object storage), circular crop recommended
│   └── Mutable: yes

email: string
├── Purpose: Contact email, may differ from user.email, visibility controlled by emailPublic toggle
│   └── Mutable: yes

phone: string
├── Purpose: Contact phone number, visibility controlled by phonePublic toggle
│   └── Mutable: yes

website: string
├── Purpose: Organizer website URL
│   └── Mutable: yes

socialLinks: object
├── Purpose: Object with social media URLs: {twitter, instagram, linkedin, youtube}
│   └── Mutable: yes

brandingColors: object
├── Purpose: Customizable branding: {primaryColor: '#hex', accentColor: '#hex'}
│   └── Mutable: yes

isPublic: boolean
├── Purpose: Profile visibility toggle, defaults to true
│   ├── Constraints: required
│   └── Mutable: yes

emailPublic: boolean
├── Purpose: Whether to show email on public profile, defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

phonePublic: boolean
├── Purpose: Whether to show phone on public profile, defaults to false
│   ├── Constraints: required
│   └── Mutable: yes

notificationPreferences: object
├── Purpose: Object with notification settings: {ticketSales: boolean, eventReminders: boolean, platformUpdates: boolean}
│   └── Mutable: yes

totalEventsCreated: number
├── Purpose: Denormalized count of events created by this organizer, updated on event creation/deletion
│   ├── Constraints: required
│   └── Mutable: yes

totalTicketsSold: number
├── Purpose: Denormalized count of tickets sold across all events, updated on ticket purchase
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Profile creation timestamp
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Last profile update timestamp
    ├── Constraints: required
    └── Mutable: yes

LARAVEL POLICIES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | UUID primary key, auto-generated |
| userId | string | Yes | Foreign key reference to users.id, indexed for fast lookups |
| displayName | string | Yes | Organizer's public display name, can differ from user.name |
| bio | string | No | Organizer bio/description, max 500 characters |
| avatarUrl | string | No | URL to organizer avatar image (stored in S3/object storage), circular crop recommended |
| email | string | No | Contact email, may differ from user.email, visibility controlled by emailPublic toggle |
| phone | string | No | Contact phone number, visibility controlled by phonePublic toggle |
| website | string | No | Organizer website URL |
| socialLinks | object | No | Object with social media URLs: {twitter, instagram, linkedin, youtube} |
| brandingColors | object | No | Customizable branding: {primaryColor: '#hex', accentColor: '#hex'} |
| isPublic | boolean | Yes | Profile visibility toggle, defaults to true |
| emailPublic | boolean | Yes | Whether to show email on public profile, defaults to false |
| phonePublic | boolean | Yes | Whether to show phone on public profile, defaults to false |
| notificationPreferences | object | No | Object with notification settings: {ticketSales: boolean, eventReminders: boolean, platformUpdates: boolean} |
| totalEventsCreated | number | Yes | Denormalized count of events created by this organizer, updated on event creation/deletion |
| totalTicketsSold | number | Yes | Denormalized count of tickets sold across all events, updated on ticket purchase |
| createdAt | timestamp | Yes | Profile creation timestamp |
| updatedAt | timestamp | Yes | Last profile update timestamp |

## Relationships


## Indexes


---
Generated by VisualPRD