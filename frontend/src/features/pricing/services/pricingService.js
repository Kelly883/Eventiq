export const pricingService = {
  getPricingWindows: async (eventId) => {},
  createPricingWindow: async (eventId, data) => {},
  updatePricingWindow: async (eventId, windowId, data) => {},
  deletePricingWindow: async (eventId, windowId) => {},
  getPricingPreview: async (eventId) => {},
};
