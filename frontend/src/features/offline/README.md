# Offline (frontend)

This feature is being bootstrapped as **mobile-first offline infrastructure**:

- Local persistence for queued operations
- Retry scheduling
- Sync client for reconciliation with backend OfflineSync
- Types for queueable domain operations

See `hooks/`, `services/`, `storage/`, `types/` (to be populated).

