export const analyticsService = {
  getAnalyticsSummary: async (eventId) => {},
  getSalesVelocity: async (eventId) => {},
  getDetailedAnalytics: async (eventId) => {},
  getEventComparison: async (eventIds) => {},
};
